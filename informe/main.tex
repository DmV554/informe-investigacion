% !TeX root = main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% ------------------------------ %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% | Formato escuela - INF PUCV | %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% | by Sebastian Garcia        | %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% | @sebaignacioo              | %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% |            2023            | %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% ------------------------------ %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
%  TRABAJO DE INVESTIGACIÓN 2026 · ICI-5444
%  Empresa 6 · TERABYTE · Tema TI-05 · Serverless y computación en el borde
%
%  ARCHIVO ÚNICO. Todo el informe vive en este main.tex, en el orden
%  definitivo acordado por el equipo (ver Terabyte-Guia-Equipo.md).
%  Cada bloque lleva en un comentario: responsable, presupuesto de
%  extensión y recordatorios de las Indicaciones (FEP00.3.26).
%
%  Buscar "%% >>>" para saltar de bloque en bloque.
%  Buscar "TODO" para ver lo que falta.
%
%  Preámbulo, clase y estilos sin cambios respecto de la plantilla.
%  La versión anterior (un .tex por sección) quedó en _legacy/.
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[12pt, letterpaper, table, xcdraw]{inf-pucv}

\usepackage{import}
\usepackage{styles}
\usepackage{subcaption}
\usepackage{pdflscape}   % \begin{landscape} para el cuadro comparativo (3.2)
\usepackage{placeins}    % \FloatBarrier: impide que una tabla salte de subsección

%% \anexo{X}{Título}: abre un anexo (o documento adjunto) con prefijo X.
%% Reinicia la numeración de páginas (X1, X2, ...) y la de tablas y figuras
%% (Tabla X.1, Figura X.1) para que no hereden la del capítulo 5.
\makeatletter
\newcommand{\anexo}[2]{%
    \clearpage
    \setcounter{page}{1}%
    \renewcommand{\thepage}{\hspace*{1em}#1\arabic{page}}%
    \setcounter{table}{0}\setcounter{figure}{0}%
    \renewcommand{\thetable}{#1.\arabic{table}}%
    \renewcommand{\thefigure}{#1.\arabic{figure}}%
    \phantomsection
    \addcontentsline{toc}{section}{#2}%
    \section*{#2}%
    \def\@currentlabel{#1}%  \ref a un \label puesto tras \anexo imprime la letra
}
\makeatother

\renewcommand{\cftdotsep}{4}
\renewcommand{\cftfigpresnum}{\bfseries Figura }
\cftsetindents{figure}{0em}{5.5em}
\renewcommand{\cfttabpresnum}{\bfseries Tabla }
\cftsetindents{table}{0em}{5.5em}
\setlength{\parindent}{1cm}

\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> ÍNDICE DE BLOQUES (orden definitivo)
%%
%%   Portada e índice general
%%   Resumen ejecutivo ........................... Daniel
%%   Introducción y contexto ..................... Daniel   (cierra con párrafo de aporte)
%%   1  Marco teórico y conceptual ............... Matías
%%   2  Desarrollo y análisis técnico
%%      2.1 Modelos de ejecución y aislamiento ... Matías
%%      2.2 Arranque en frío ..................... Isidora
%%      2.3 Facturación y punto de equilibrio .... Francisca
%%      2.4 Diseño orientado a eventos ........... Valentina
%%      2.5 WebAssembly y WASI ................... Isidora
%%      2.6 Límites y dependencia del proveedor .. Fabián
%%   3  Análisis comparativo de tecnologías y productos
%%      3.1 Alternativas adicionales ............. Claudio y Daniel
%%      3.2 Criterios y cuadro comparativo ....... Valentina (criterios), Rodolfo (AWS/Azure,
%%                                                  consolida), Claudio y Daniel (resto)
%%   4  Caso de estudio e implementación práctica
%%      4.1 Prueba de concepto ................... Vicente
%%      4.2 Modelo de costos ..................... Francisca
%%      4.3 Caso de licitación TERABYTE .......... Fabián
%%   5  Tendencias y riesgos ..................... Fabián
%%   Conclusiones y recomendación ................ todo el grupo
%%   Bibliografía ................................ todos (>= 10 académicas + técnicas)
%%   Anexo A  Declaración de uso de IA ........... Daniel recopila; cada uno declara lo suyo
%%   Anexo B  Código y mediciones de la PoC ...... Vicente
%%   Anexo C  Planilla del modelo de costos ...... Francisca
%%   Anexo D  Búsqueda documentada de alternativas Claudio y Daniel
%%   Documento adjunto: cuestionario ............. todos (3-4 preguntas c/u); Daniel arma índice
%%
%% Presupuesto total del cuerpo (resumen -> conclusiones): 10 a 15 páginas.
%% Referencia: 1 página de texto corrido ~ 450 palabras.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> PORTADA
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\thispagestyle{empty}
\pagenumbering{gobble}

\begin{center}
    \membrete{%
        PONTIFICIA UNIVERSIDAD CATÓLICA DE VALPARAÍSO\\
        FACULTAD DE INGENIERÍA\\
        ESCUELA DE INGENIERÍA INFORMÁTICA\\
    }

    \vspace*{3.5cm}

    \tituloPortada{%
        Serverless y computación \\
        en el borde
    }

    \vspace*{2.5cm}

    \alumnos{%
        Francisca Antonia Abarca Pezo\\
        Vicente André Arratia Caroca\\
        Isidora Valentina Cisternas Cisternas\\
        Rodolfo Antonio Fernández Vera\\
        Valentina Ignacia Guzmán Elgueta\\
        Daniel Ignacio Miranda Vázquez\\
        Matias Ignacio Reyes Pizarro\\
        Fabián Andres Solís Piñones\\
        Claudio Patricio Toledo Mac-lean\\
    }

    \vspace*{1.4cm}

    \datosPortada{%
        \datoPortada{Empresa}{N.\textordmasculine\ 6 --- TERABYTE}
        \datoPortada{Tema}{TI-05}
        \datoPortada{Asignatura}{Taller de Formulación de Proyectos Informáticos}
        \datoPortada{Fecha de entrega}{21 de septiembre de 2026}
    }

    \vspace*{1.2cm}

    \fechaPortada{Septiembre de 2026}
\end{center}

\clearpage

%% >>> ÍNDICE GENERAL
\tableofcontents
\clearpage

%% Listas de figuras y tablas: PENDIENTE de decisión del equipo.
%% Descomentar si se decide incluirlas.
% \phantomsection
% \addcontentsline{toc}{section}{Lista de figuras}
% \listoffigures
% \clearpage
% \phantomsection
% \addcontentsline{toc}{section}{Lista de tablas}
% \listoftables
% \clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> RESUMEN EJECUTIVO                                  Responsable: Daniel
%% Presupuesto: 0,5 página / ~200 palabras. Se escribe AL FINAL.
%% Contenido: qué se comparó, resultado principal (volumen de equilibrio,
%% latencia medida), recomendación en una frase. Sin contexto ni definiciones.
%% IA: solo corrección de estilo (nivel 1), declarada en Anexo A.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\pagenumbering{arabic}
\setcounter{page}{1}

\phantomsection
\addcontentsline{toc}{section}{Resumen ejecutivo}
\section*{Resumen ejecutivo}

%% [BORRADOR IA — Daniel reescribe con sus palabras antes de la entrega.]
%% Las cifras vienen de las secciones citadas; al reescribir, volver a
%% cotejarlas con 4.1, 4.2 y 4.3 por si cambian.

Este informe compara cuatro modelos de ejecución en la nube, funciones como servicio, contenedores sin servidor, cómputo sin servidor en el borde e instancias siempre encendidas, en las tres dimensiones que fija la ficha del tema TI-05: costo, latencia y complejidad operacional. La pregunta que responde es para qué tipo de carga conviene cada modelo y qué implica esa elección para la propuesta técnica del grupo en la licitación del terminal portuario TERABYTE.

El desarrollo (sección~\ref{sec:modelos} a~\ref{sec:limites_dependencias}) muestra que los cuatro modelos se diferencian menos por lo que ejecutan que por cómo aíslan, cómo cobran y qué límites imponen: el mecanismo de aislamiento fija el arranque en frío, la unidad de cobro decide si el tiempo ocioso se paga, y los topes de tiempo, memoria y estado determinan qué aplicaciones deben rediseñarse. El cuadro comparativo (sección~\ref{sec:cuadro}) reúne doce plataformas con precio de lista fechado y límites publicados, sin puntuar la complejidad operacional porque la literatura no ofrece una métrica que lo respalde; a las plataformas de la ficha se suman diez alternativas seleccionadas con criterios explícitos entre treinta y tres candidatos (sección~\ref{sec:alternativas} y Anexo~\ref{anexo:busqueda}).

Las mediciones propias (sección~\ref{sec:poc}) cuantifican el arranque en frío desde un cliente en Chile: la penalización mediana fue de 395~ms en AWS Lambda, 720~ms en Google Cloud Run y 1~ms en Cloudflare Workers, orden que sigue al mecanismo de aislamiento. El modelo de costos (sección~\ref{sec:costos}) sitúa el volumen de equilibrio entre AWS Lambda y un par de instancias reservadas en 24,0 millones de solicitudes mensuales en el escenario base, con un rango que va de 5,3 millones a nunca cruzar según memoria, duración y tipo de reserva. Aplicado al caso (sección~\ref{sec:caso}), el tráfico del terminal queda muy por debajo de ese cruce, pero la exigencia de respuesta bajo un segundo en la cabina de grúa y de 72~horas de autonomía sin enlace descarta un núcleo dependiente de invocaciones remotas.

La recomendación es reservar el modelo sin servidor para las cargas intermitentes, orientadas a eventos y tolerantes al arranque en frío, como el portal público y las notificaciones, y mantener el núcleo operacional sobre capacidad siempre encendida y contenedores gestionados, auditando en la etapa de diseño los caminos de facturación que crecen con el tráfico (sección~\ref{sec:riesgos}).

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> INTRODUCCIÓN Y CONTEXTO                            Responsable: Daniel
%% Presupuesto: 0,75 página / ~350 palabras. Se escribe AL FINAL.
%% Contenido: el problema (serverless/edge vs. always-on en costo, latencia y
%% complejidad operacional), el proyecto de licitación de TERABYTE como caso,
%% cómo se organiza el informe. Sin historia del cloud.
%%
%% PÁRRAFO FINAL OBLIGATORIO (punto 4 Indicaciones): distinguir qué viene de
%% la ficha TI-05 y qué es aporte del grupo. Enumerar: alternativas
%% adicionales (3.1), PoC con mediciones propias (4.1), aplicación al caso
%% TERABYTE (4.3), riesgos y evidencia contradictoria (5), recomendación
%% comprometida. El profesor usa este párrafo para evaluar el análisis crítico.
%% IA: prohibida en el párrafo de aporte (punto 6.1).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\phantomsection
\addcontentsline{toc}{section}{Introducción y contexto}
\section*{Introducción y contexto}

%% [BORRADOR IA — Daniel reescribe con sus palabras antes de la entrega.]
%% El último párrafo (identificación del aporte) está en la lista del
%% punto 6.1: el texto final debe ser del autor; este borrador solo ordena
%% qué enumerar. Declarar en Anexo A lo que corresponda.

Una aplicación en la nube puede ejecutarse sobre capacidad reservada, que se paga encendida esté o no en uso, o sobre plataformas sin servidor que asignan, escalan y facturan recursos por cada solicitud. La segunda opción promete pagar solo lo que se consume y delegar la operación al proveedor, pero introduce un retardo al crear cada entorno nuevo, límites de tiempo, memoria y estado que no existen en una máquina propia, y una dependencia de las interfaces de cada proveedor. Decidir entre ambas exige comparar, con datos verificables, cuánto cuesta cada modelo según el volumen de tráfico, cuánta latencia agrega y cuánto trabajo de operación queda del lado del equipo.

Este informe realiza esa comparación para cuatro modelos de ejecución: funciones como servicio, contenedores sin servidor, cómputo sin servidor en el borde de la red e instancias siempre encendidas, este último como línea base. El marco lo da la \emph{ficha} del tema TI-05 del documento de indicaciones del curso \parencite{moya2026indicaciones}, que fija las tres dimensiones de comparación (costo, latencia y complejidad operacional) y nombra las plataformas de partida en cinco \emph{listas} o categorías de tecnologías: funciones, contenedores, borde, plataformas de código abierto y datos sin servidor. En adelante, «ficha» y «lista» se usan con ese sentido. El caso de aplicación es la propuesta técnica del grupo para la licitación del sistema operativo del terminal portuario TERABYTE, cuyo tráfico transaccional, exigencias de latencia en la cabina de grúa y autonomía sin enlace ponen a prueba los cuatro modelos a la vez.

El documento se organiza así. La sección~\ref{sec:marco} define los términos mínimos. La sección~\ref{sec:modelos} y las que siguen analizan los mecanismos de aislamiento, el arranque en frío, la facturación por consumo y el punto de equilibrio, el diseño orientado a eventos, WebAssembly como entorno de borde y los límites y la dependencia del proveedor. La sección~\ref{sec:alternativas} amplía el conjunto de plataformas y la sección~\ref{sec:cuadro} las compara en un cuadro con precios fechados y límites publicados. La sección~\ref{sec:poc} mide el arranque en frío en tres plataformas, la sección~\ref{sec:costos} construye el modelo de costos y su volumen de equilibrio y la sección~\ref{sec:caso} aplica los resultados al caso. La sección~\ref{sec:riesgos} reúne tendencias, riesgos y evidencia en contra, y el informe cierra con una recomendación.

%% Párrafo de identificación del aporte (punto 4 de las Indicaciones).
De la ficha TI-05 provienen el tema, las tres dimensiones de comparación, las plataformas de partida y los tres entregables mínimos: el cuadro comparativo, la prueba de concepto y el modelo de costos. Es aporte del grupo lo que va más allá de ese encargo: las diez alternativas adicionales seleccionadas con criterios explícitos y trazables (sección~\ref{sec:alternativas}, Anexo~\ref{anexo:busqueda}); las mediciones propias de arranque en frío en tres plataformas desde un cliente en Chile, con código y datos adjuntos (sección~\ref{sec:poc}, Anexo~\ref{anexo:poc}); la decisión de no puntuar la complejidad operacional y su justificación bibliográfica (sección~\ref{sec:cuadro}); el análisis de sensibilidad del volumen de equilibrio (sección~\ref{sec:costos}, Anexo~\ref{anexo:costos}); la aplicación al caso TERABYTE con sus cotas de latencia y autonomía (sección~\ref{sec:caso}); la evidencia contradictoria recogida en la sección~\ref{sec:riesgos}; y la recomendación final, con la que el grupo se compromete.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 1. MARCO TEÓRICO Y CONCEPTUAL                      Responsable: Matías
%% Presupuesto: máximo 1 página / ~450 palabras.
%% Contenido: definiciones mínimas para leer el capítulo 2: serverless (qué es
%% y qué no es), FaaS, contenedores sin servidor, edge computing, instancia
%% siempre encendida, y las tres dimensiones de comparación (costo, latencia,
%% complejidad operacional). Fuentes: CNCF, papers, docs oficiales.
%% No explicar qué es un contenedor, una VM o la nube.
%% Referencias cruzadas a 2.x van con número fijo porque esas subsecciones
%% aún no tienen \label. Si se les agrega label, cambiar por \ref.
%%
%% IA: borrador revisado con apoyo de IA a partir de fuentes abiertas y
%% verificadas en el original (ver TRAZABILIDAD al final).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Marco teórico y conceptual}
\label{sec:marco}

\textbf{Computación sin servidor.} Es la ejecución de aplicaciones sin gestionar servidores: una plataforma ejecuta, escala y factura el código según la demanda de cada momento. No implica que desaparezcan los servidores ni la operación, sino que su gestión pasa al proveedor \parencite{cncf2018}. \textcite{schleiersmith2021} la caracterizan por tres cualidades: abstracción de los servidores y de su operación, cobro por uso en vez de por reserva (sin cargo por recursos ociosos) y escalamiento automático desde cero. Tampoco es sinónimo de funciones: incluye los servicios de \textit{backend} gestionados, o BaaS \parencite{golec2025coldstart}.

\textbf{Modelos de ejecución.} En las \textit{funciones como servicio} (FaaS) la unidad es una función breve, activada por un evento o una solicitud HTTP, que el proveedor escala horizontalmente \parencite{cncf2018,leitner2019} y libera tras un periodo sin uso, lo que se llama escalamiento a cero \parencite{golec2025coldstart}. En los \textit{contenedores sin servidor} la unidad es una imagen de contenedor y el equipo ya no aprovisiona ni escala máquinas virtuales \parencite{awsfargate}, aunque no siempre hay escalamiento a cero: Cloud Run retira la última instancia cuando no hay solicitudes \parencite{gcprun}, mientras que Fargate cobra hasta que la tarea termina \parencite{awsfargatepricing}. El \textit{cómputo en el borde} procesa en recursos ubicados entre la fuente de los datos y el centro de datos de la nube \parencite{shi2016}; este informe estudia su variante sin servidor \parencite{aslanpour2021}.

\textbf{Modelo de referencia.} Las \textit{instancias siempre encendidas} son capacidad aprovisionada en forma continua (máquinas virtuales, o contenedores sobre nodos fijos) que se factura por tiempo encendido, se use o no \parencite{ustiugov2021}. Con cargas en ráfagas, esa capacidad queda subutilizada \parencite{eismann2021}.

\textbf{Dimensiones de comparación.} El \textit{costo} en \textit{serverless} se calcula por consumo (invocaciones, tiempo de ejecución, memoria, CPU y transferencia de datos) \parencite{ghorbian2026}, de modo que el tiempo ocioso no se paga \parencite{gojko_adzic_serverless_2017}; en el modelo siempre encendido es casi fijo para una capacidad dada. Esa diferencia define un volumen de equilibrio (secciones~2.3 y~\ref{sec:costos}). La \textit{latencia} es el tiempo entre el envío de una solicitud y su respuesta, suma de la espera en cola, la asignación de recursos y la ejecución \parencite{golec2025coldstart}. Al escalar a cero se agrega el \textit{arranque en frío}, el retardo de preparar un entorno nuevo \parencite{ustiugov2021}, tratado en las secciones~2.2 y~\ref{sec:poc}; el borde, en cambio, reduce la latencia de red al acercar el cómputo a la fuente de los datos \parencite{golec2025coldstart}. La \textit{complejidad operacional} es el trabajo del equipo para desplegar, escalar, monitorear y mantener el sistema. El modelo sin servidor traslada parte de ese trabajo al proveedor, un motivo frecuente de adopción \parencite{eismann2021}, pero \textcite{leitner2019} documentan herramientas de prueba y despliegue poco maduras y la necesidad de componer servicios del proveedor, a lo que se suman sus límites y la dependencia (secciones~2.4 y~2.6). Cómo se mide cada dimensión se justifica en la sección~\ref{sec:cuadro}.

%% ---------------------------------------------------------------------------
%% TRAZABILIDAD (qué respalda cada cita):
%%  cncf2018 ........ definición textual de serverless; "does not mean that we
%%                    no longer use servers"; FaaS "triggered by events or
%%                    HTTP requests"; BaaS.
%%  schleiersmith2021 tres "essential qualities" (abstracción, pay-as-you-go vs.
%%                    reservation-based, escalamiento desde cero).
%%  golec2025coldstart ....... (PDF del proyecto) serverless integra BaaS y FaaS (§1);
%%                    scale to zero y cold start (§1, §3.3); definición de
%%                    latencia como cola + asignación + ejecución (§3.2);
%%                    edge acerca el cómputo a la fuente (§1).
%%  leitner2019 ..... funciones "atomic and short-running" escaladas
%%                    horizontalmente; herramientas de prueba/despliegue poco
%%                    maduras; FaaS como "glue" de servicios (abstract).
%%  awsfargate ...... "no longer have to provision, configure, or scale
%%                    clusters of virtual machines".
%%  gcprun .......... "even the last remaining instance will be removed".
%%  awsfargatepricing cobro desde "download your container image" hasta que
%%                    "the task terminates" (no se cita ningún precio).
%%  shi2016 ......... definición de "edge" (§II).
%%  aslanpour2021 ... serverless edge computing (abstract).
%%  ustiugov2021 .... (PDF del proyecto) VMs "billed for their up-time
%%                    regardless of usage"; cold start tras inactividad (§1, §2.1).
%%  eismann2021 ..... ahorro en cargas en ráfagas con baja utilización en
%%                    hosting tradicional; menos carga operacional (deployment,
%%                    scaling, monitoring) como motivo de adopción.
%%  ghorbian2026 .... (PDF del proyecto) métricas de precio: execution time,
%%                    memory, request count, CPU, bandwidth (§2.3).
%%  gojko_adzic_serverless_2017 ....... "application idle time is free".
%% ---------------------------------------------------------------------------

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 2. DESARROLLO Y ANÁLISIS TÉCNICO
%% Seis subtemas obligatorios de la ficha TI-05.
%% Presupuesto: 0,75 página / 300-350 palabras POR SUBSECCIÓN.
%% Estructura sugerida de cada subsección (tres párrafos):
%%   1) el concepto, con fuente;
%%   2) cómo se comporta en cada modelo (FaaS / contenedor serverless / edge /
%%      always-on), con fuente;
%%   3) "cuándo conviene": la conclusión comparativa del subtema.
%% Nada de descripciones de productos, capturas ni listas de características.
%% Los precios NO se buscan aquí: se citan desde la Tabla 3.2.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Desarrollo y análisis técnico}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 2.1 MODELOS DE EJECUCIÓN Y AISLAMIENTO                BORRADOR
%%
%% Extensión: párrafos 1 y 2 = ~320 palabras. Con el párrafo 3 (50-70
%% palabras, autoría humana) queda en ~380, un poco sobre el presupuesto de
%% 300-350. Si falta espacio, lo primero que podemos sacar sin perder el hilo
%% es la frase de Cloud Run functions (\parencite{gcprunfunctions}).
%%
%% Referencias cruzadas a 2.2 y 2.5 van con número fijo porque esas
%% subsecciones aún no tienen \label (mismo criterio del marco teórico).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Modelos de ejecución y aislamiento}
\label{sec:modelos}

Además de la unidad que ejecuta (sección~\ref{sec:marco}), cada modelo se distingue por cómo aísla a los clientes que comparten una máquina \parencite{shafiei2022}. Compartir servidores permite vender tramos cortos de capacidad que de otro modo quedaría ociosa \parencite{liu2024demystifying}, pero multiplica el costo de toda sobrecarga de aislamiento \parencite{agache2020}. \textcite{agache2020} agrupan los mecanismos en tres familias: los \textit{contenedores}, que comparten el núcleo del anfitrión y obligan a transar entre seguridad y compatibilidad; la \textit{virtualización}, que da a cada carga su propio núcleo y aísla con fuerza, pero ocupa más memoria y tarda segundos en arrancar \parencite{wen2023}; y el \textit{aislamiento por lenguaje}, como los \textit{isolates} de V8, que comparten un proceso a costa de compatibilidad y de exposición a ataques de canal lateral. En medio se ubican las microVM, máquinas virtuales mínimas, y gVisor, un núcleo en espacio de usuario que atiende las llamadas al sistema del contenedor \parencite{wang2022}.

En FaaS, AWS Lambda crea cada entorno en una microVM de Firecracker que no reutiliza entre funciones ni entre cuentas \parencite{awslambdasec}; según sus creadores, Firecracker agrega menos de 5~MB por microVM, arranca en menos de 125~ms, ejecuta binarios Linux sin modificar y sostiene también a Fargate \parencite{agache2020}. Cloud Run, base de las Cloud Run functions \parencite{gcprunfunctions}, ofrece un entorno basado en gVisor, de arranque rápido pero que no emula todas las llamadas al sistema, y otro basado en microVM, totalmente compatible con Linux y con mejor CPU y red, pero de arranque más lento en algunos servicios \parencite{gcprunenv}; ambos suman una capa de virtualización por hardware \parencite{gcprunsec}. En pruebas independientes, gVisor aisló mejor el rendimiento, pero penalizó más las llamadas al sistema y la red \parencite{wang2022}. En el borde, con nodos de pocos recursos donde VM y contenedores resultan pesados \parencite{xie2021}, Cloudflare Workers ejecuta miles de \textit{isolates} por máquina, solo acepta JavaScript y WebAssembly (sección~2.5) y, contra Spectre, restringe relojes e hilos \parencite{cfworkerssec}. En el modelo siempre encendido, la frontera es la VM arrendada: compatibilidad total \parencite{agache2020} y recursos reservados aunque estén ociosos \parencite{liu2024demystifying}.

Ningún mecanismo aventaja a los demás en todos los frentes a la vez, ya que seguridad, compatibilidad, densidad y velocidad de arranque se transan entre sí, tal como concluyen tanto \textcite{agache2020} como \textcite{wang2022}, quienes afirman textualmente que «ningún runtime es ventajoso en todos los aspectos». Google mismo reconoce que su entorno de segunda generación (microVM) «generalmente rinde más bajo carga sostenida», pero arranca más lento que el basado en gVisor en ciertos servicios \parencite{gcprunenv}, lo que entrega un criterio directo ligado a la condición de carga. Los \textit{isolates} de V8 solo admiten JavaScript y WebAssembly, sin binarios nativos \parencite{cfworkerssec}, mientras que Firecracker sí ejecuta binarios Linux sin modificar \parencite{agache2020}, lo que marca la restricción de compatibilidad de código. \textcite{wang2022} también muestran que gVisor penaliza especialmente las llamadas al sistema, la red y los archivos pequeños. Y aunque Firecracker arranca en menos de 125~ms, Lambda igual mantiene un \textit{pool} de microVM prearrancadas, porque ese tiempo no alcanza para el escalamiento en ráfaga. La elección entre contenedores, microVM, isolates o VM completa depende, entonces, del tipo de código que se ejecutará (lenguaje, binarios, llamadas al sistema), del patrón de carga (ráfagas cortas versus carga sostenida) y de si el costo de mantener recursos siempre listos compensa evitar la latencia de arranque.

%% ---------------------------------------------------------------------------
%% OPCIONAL: Claude me tiro hacer una tabla resumen (1/4 de página). que nos seria util para la presentación y
%% para el cuestionario, incluirla en el informe solo si cabe en 15 páginas.
%% todas las cifras salen de las fuentes citadas en cada fila.
%% ---------------------------------------------------------------------------
% \tabla{Mecanismos de aislamiento en plataformas sin servidor y de borde}{aislamiento}{|p{2.6cm}|p{3.3cm}|p{4.4cm}|p{3.6cm}|}{%
%     \filaTabla{\textbf{Mecanismo} & \textbf{Frontera entre clientes} & \textbf{Ventaja y costo} & \textbf{Dónde se usa}}
%     \filaTabla{microVM (Firecracker) & Núcleo propio bajo un monitor de VM mínimo & $<$5~MB de sobrecarga, $<$125~ms de arranque, binarios Linux sin cambios \parencite{agache2020} & Lambda, Fargate; Cloud Run 2.\textsuperscript{a} gen. \parencite{gcprunenv}}
%     \filaTabla{Núcleo de aplicación (gVisor) & Llamadas al sistema atendidas en espacio de usuario & Arranque rápido; no emula todas las llamadas; sobrecarga en llamadas al sistema y red \parencite{gcprunenv,wang2022} & Cloud Run 1.\textsuperscript{a} gen.}
%     \filaTabla{Isolate V8 & Memoria separada dentro de un proceso compartido & Miles por máquina; solo JavaScript y WebAssembly \parencite{cfworkerssec} & Cloudflare Workers}
%     \filaTabla{VM completa & Máquina virtual arrendada & Compatibilidad total; recursos reservados aunque estén ociosos \parencite{agache2020,liu2024demystifying} & Modelo siempre encendido}
% }

%% ---------------------------------------------------------------------------
%% 2.2                                                    Responsable: Isidora
%% Causas (carga del entorno, init del runtime, aislamiento), medición,
%% mitigaciones (concurrencia aprovisionada, instantáneas/SnapStart, entornos
%% livianos, min instances). Cierra citando los resultados propios:
%% "las mediciones del grupo (sección 4.1) ..." -> \ref{sec:poc}.
%% ---------------------------------------------------------------------------
\subsection{Arranque en frío: causas, medición y mitigaciones}
\label{sec:coldstart}
\label{subsec:arranque_en_frio}

El escalado a cero libera los entornos inactivos; según mediciones de 2021, AWS desaloja la mitad de sus contenedores cada 380~s~\cite{copik2021sebs}. Sin instancias disponibles, la plataforma debe reservar recursos, crear el entorno aislado (ver~2.1), cargar runtime y dependencias y ejecutar la inicialización~\cite{golec2025coldstart}: esa latencia es el arranque en frío; reutilizar una instancia preparada es un arranque en caliente. Crece con dependencias grandes y runtimes pesados como la JVM o .NET, frente a Python o Node.js~\cite{golec2025coldstart}, y aparece también al escalar de $N$ a $N+1$ instancias en ráfagas~\cite{giannini2022cpuboost}.

Medirlo exige separar inicialización y ejecución comparando invocaciones frías y calientes, como hace Invoc-Overhead~\cite{copik2021sebs}. AWS Lambda registra esa Init Duration y, desde agosto de 2025, la factura en todas las configuraciones: el arranque en frío también es costo~\cite{gupta2025initbilling}.

Las mitigaciones actúan sobre distintas partes del proceso y difieren en costo. La concurrencia aprovisionada lo evita con instancias ya inicializadas (AWS, Cloud Run, Azure Functions)~\cite{aws2023provisionedconcurrency, googlecloudrunmininstances, azurefunctionsflex}, pero paga capacidad ociosa, acercándose al modelo siempre encendido (ver~2.3 y~3.2), y no cubre el paso de $N$ a $N+1$, que Google aborda con startup CPU boost~\cite{giannini2022cpuboost}. Las instantáneas, como SnapStart, se saltan la inicialización restaurando una captura cifrada de la microVM~\cite{aws2022snapstart}, aunque parte del retraso pasa a la ejecución al cargarse la memoria desde disco~\cite{ustiugov2021}. Los entornos livianos (V8, WebAssembly; ver~2.5) abaratan la creación del entorno: en un prototipo, Wasm redujo el arranque en frío cerca de un 94\,\% frente a Docker~\cite{gackstatter2022pushing}, pero exige adaptar el código.

El impacto depende del patrón de carga~\cite{copik2021sebs}. En APIs síncronas, donde degrada la experiencia de usuario, sirven las instantáneas (con costo de caché en Python y .NET)~\cite{aws2022snapstart} o los aislamientos livianos, cambiando de plataforma~\cite{gackstatter2022pushing}; aun así, Cloudflare afirmó en 2020 haberlo eliminado~\cite{cloudflare2020workers}, pero reapareció con scripts más pesados y su técnica de sharding lo redujo 10 veces~\cite{cloudflare2025shard}. En tráfico sostenido, la concurrencia aprovisionada es más económica sobre el 60\,\% de uso continuo~\cite{gupta2025initbilling}. En procesos asíncronos, fuera de la ruta crítica, la latencia es tolerable: es despreciable en funciones de más de 10~s y, sin alta utilización, escalar a cero cuesta menos que una máquina virtual~\cite{copik2021sebs}.%% ---------------------------------------------------------------------------
%% 2.3                                                    Responsable: Francisca
%% GB-segundo, vCPU-segundo, tiempo de CPU vs. tiempo de reloj, número de
%% invocaciones, salida de datos (egress); concepto de punto de equilibrio
%% frente a instancias reservadas / contenedores siempre encendidos.
%% El cálculo numérico va en 4.2 (\ref{sec:costos}); aquí los conceptos y
%% variables. Toda cifra que aparezca se toma de la Tabla 3.2 con su fecha.
%% ---------------------------------------------------------------------------
\subsection{Facturación por consumo y punto de equilibrio}
\label{sec:facturacion}

Según \textcite{lin_demystifying_2026}, la factura de una plataforma sin servidor combina cuatro factores: el tiempo de reloj facturable, los recursos asignados o consumidos durante ese tiempo, la granularidad o el mínimo de cobro y un cargo fijo por invocación. Las funciones cobran invocaciones y duración ponderada por la memoria asignada; AWS Lambda redondea al milisegundo y, desde agosto de 2025, factura también la fase de inicialización (\cite{Shubham_aws_2025}), mientras que Azure Functions ofrece un plan Premium con instancias precalentadas cobrado por núcleo-segundo y memoria (\cite{microsoft2026functionsscale}). Los contenedores sin servidor cobran vCPU y memoria por segundo: Fargate con un mínimo de un minuto por tarea (\cite{aws_precios2026}) y Cloud Run por solicitud o por instancia, a elección (\cite{google2026cloudrunpricing}). En el borde, Cloudflare Workers cobra solicitudes y tiempo de CPU consumido, no de reloj, y no cobra transferencia de datos (\cite{aws_cloudflare_2026}). Los valores numéricos se remiten en las Tablas 3.2a y 3.2b (sección~\ref{sec:cuadro}).

Como cada modelo mide una unidad distinta, la CPU facturada es entre 1,01 veces (Workers) y 3,63 veces (Google) la consumida en simulaciones sobre trazas de Huawei Cloud (\cite{lin_demystifying_2026}). El cobro por reloj incluye la espera por E/S (por ejemplo, esperar a que responda una base de datos), y encadenar funciones duplica esa facturación \cite{eismann2021}. En AWS Lambda, Google e IBM incluyen también la inicialización  (\cite{lin_demystifying_2026}). Workers cobra la CPU consumida sin contar la duración, con un tope de 128 megabytes por {\itshape Isolate} (\cite{cloudflare2026workerslimits}). Por eso las unidades solo se comparan normalizadas (Tablas 3.2a y 3.2b).

El punto de equilibrio es la carga desde la cual pagar por consumo cuesta más que una instancia siempre encendida. Pagar por consumo conviene mientras la prima de precio sea menor que la razón entre tiempo contratado y tiempo usado (\cite{xiayue_charles_lin_serverless_2020,copik2021sebs}). El cruce llega antes con carga regular que en ráfagas, y todos los patrones cruzan el punto de equilibrio si el volumen crece (\cite{liu2024demystifying}). La capacidad fija solo conviene si se mantiene una alta utilización (\cite{copik2021sebs}). Sin variar el volumen, el cruce se desplaza con los descuentos por reserva (37,5\% a un año y 59\% a tres años en EC2, derivados de la Tabla 3.2a), la concurrencia aprovisionada (\cite{lin_demystifying_2026}) y costos como mantenimiento y egreso (\cite{hamza2024costdynamics}). La evidencia es frágil: dos casos anecdóticos (\cite{gojko_adzic_serverless_2017}), precios de 2020 (\cite{xiayue_charles_lin_serverless_2020}) (\cite{copik2021sebs}) y trazas de un solo proveedor (\cite{lin_demystifying_2026}). El detalle de su cuantificación está en la sección~\ref{sec:costos}.
%% ---------------------------------------------------------------------------
%% 2.4                                                    Responsable: Valentina
%% Colas, publicación/suscripción, idempotencia, entrega al menos una vez,
%% orquestación frente a coreografía. Aquí también se discute la complejidad
%% operacional (observabilidad, depuración distribuida). Candidatos "otros"
%% de datos serverless -> pasar a Claudio y Daniel (3.1).
%% ---------------------------------------------------------------------------
\subsection{Diseño orientado a eventos}
\label{sec:eventos}

Una arquitectura orientada a eventos construye sistemas débilmente acoplados que
colaboran emitiendo y respondiendo a eventos en lugar de invocarse entre sí
\parencite{awseventbridge}. CloudEvents define el evento como un registro de
datos que expresa una ocurrencia y su contexto, enrutado hacia los consumidores
interesados sin identificar un destino \parencite{cloudevents102}. Para
\textcite{eugster2003}, la fortaleza del estilo está en el desacoplamiento
simultáneo en espacio, tiempo y sincronización entre publicador y consumidor.

La cola punto a punto entrega cada mensaje a un único consumidor, con semántica
\emph{uno-de-n}, y al extraerse de forma síncrona no desacopla en sincronización
\parencite{eugster2003}. En publicación/suscripción, suscribirse a un tópico
equivale a integrar un grupo y publicar, a difundirlo entre sus miembros
\parencite{eugster2003}. El patrón fija quién recibe el evento; la semántica de
entrega, cuántas veces llega y qué debe tolerar el consumidor. Las dos variantes
de SQS lo muestran: la estándar entrega al menos una vez y obliga a manejar
duplicados con operaciones idempotentes, la FIFO procesa exactamente una vez
\parencite{awssqsqueuetypes}. El RFC 9110 llama idempotente al método cuyo efecto
pretendido sobre el servidor no cambia al repetir la petición
\parencite{rfc9110}. En las fuentes revisadas el exactamente-una-vez nunca
aparece sin condiciones: Kafka lo alcanza sumando al productor idempotente el
envío transaccional a varias particiones \parencite{kafkadesign}; Pub/Sub, solo
dentro de una región y en suscripciones \emph{pull}
\parencite{gcppubsubexactlyonce}.

La coordinación admite dos formas opuestas: la orquestación centraliza el orden
de los pasos y la coreografía deja que cada servicio reaccione por su cuenta a
los eventos ajenos. La guía prescriptiva de AWS (documentación técnica de
terceros, declarada como tal) recomienda coreografía dentro de un microservicio y
orquestación al cruzar ese límite, con el patrón \emph{saga} para revertir
transacciones distribuidas \parencite{awscoordination}. El criterio es el costo
del error: MQTT reserva QoS~0 a sensores ambientales, donde perder una lectura no
importa, y QoS~2 a facturación, donde un duplicado es un cobro incorrecto
\parencite{mqttv5}; Step Functions limita el exactamente-una-vez a acciones no
idempotentes de larga duración y deja los flujos Express, al menos una vez, para
alto volumen ya idempotente \parencite{awsstepfunctions}. En esa última ruta la
plataforma no gestiona la idempotencia por el consumidor: uno que no lo sea
devuelve resultados incorrectos al primer reintento.
% TODO (Valentina)

%% ---------------------------------------------------------------------------
%% 2.5                                                    Responsable: Isidora
%% Portabilidad, aislamiento y arranque submilisegundo. WASI desde W3C /
%% Bytecode Alliance; Fermyon Spin / SpinKube, wasmCloud, Wasmtime.
%% Candidatos "otros" de edge y open source -> pasar a Claudio y Daniel (3.1).
%% ---------------------------------------------------------------------------
\subsection{WebAssembly y WASI como entorno de borde}
\label{sec:wasm_wasi}

WebAssembly (Wasm) es una abstracción portable sobre el hardware, de ejecución eficiente, segura e independiente de la plataforma~\cite{haas2017bringing, w3c2026wasmcore}. Aísla cada módulo en una memoria lineal separada del código y de la pila, y en arquitecturas de 64~bits usa páginas de guarda que delegan la verificación de límites al hardware (MMU)~\cite{haas2017bringing}. Además, carece de acceso ambiental al host: toda interacción externa requiere importaciones explícitas~\cite{w3c2026wasmcore}. Fuera del navegador, el WASI Subgroup del WebAssembly Community Group del W3C estandariza esa interfaz de sistema sobre el Component Model y WIT, con seguridad por capacidades~\cite{wasi2026releases, gohman2024wasi, wasi2024p2}. WASI~0.3 añadió asincronismo nativo, y Wasmtime~46 fue la primera versión en implementar su especificación final~\cite{wasi2026releases, wasi2026p3}. El ecosistema incluye runtimes como Wasmtime, orquestadores como wasmCloud y frameworks como Spin~\cite{cncfwasmcloud, spinframework}.

A diferencia del contenedor, que comparte el kernel pero empaqueta el espacio de usuario, Wasm aísla a nivel de bytecode dentro del mismo proceso, con una huella mínima y un arranque rápido~\cite{haas2017bringing, kjorveziroski2021iot}. Mediciones revisadas por pares sitúan ese arranque bajo 10~ms con menos de 200~KB, y en cientos de microsegundos al restaurar instantáneas preinicializadas (sección~2.2)~\cite{shillaker2020faasm}. El costo es el rendimiento sostenido: sobre SPEC CPU, Wasm fue en promedio entre 1{,}45$\times$ (Firefox) y 1{,}55$\times$ (Chrome) más lento que el código nativo~\cite{jangda2019notsofast}. Como son mediciones de 2019 en navegadores y no en runtimes de servidor, deben leerse como orden de magnitud. En el borde, Fastly Compute ejecuta Wasm sobre Wasmtime~\cite{fastlycomputeintro} y Cloudflare Workers lo admite dentro de isolates de V8~\cite{cloudflarehowworkersworks}, pero ambos exigen APIs propias: hostcalls en Fastly~\cite{fastlycomputesdks} y un soporte de WASI parcial y experimental en Cloudflare~\cite{cloudflare2026wasm}. Así, la portabilidad del runtime no garantiza la de la aplicación (sección~2.6), y la mayoría de las toolchains aún apunta a WASI~0.2~\cite{wasi2026releases}.

Wasm conviene en cargas intermitentes u orientadas a eventos en el borde, donde su arranque y su huella aumentan la densidad de instancias por servidor y reducen costos mediante un escalado a cero efectivo~\cite{kjorveziroski2021iot, shillaker2020faasm}. No conviene en cómputo intensivo de larga duración, donde los contenedores siempre encendidos mantienen superioridad en throughput~\cite{jangda2019notsofast}, ni en proyectos que exigen estricta portabilidad entre plataformas, por las hostcalls propietarias, el soporte parcial de WASI y la madurez desigual de las toolchains~\cite{wasi2026releases, fastlycomputesdks, cloudflare2026wasm}.
\subsection{Límites técnicos y dependencia del proveedor}
\label{sec:limites_dependencias}

Toda plataforma serverless multiusuario acota tiempo, memoria y payload para proteger a los demás usuarios y sostener el escalado a cero. AWS Lambda está diseñado para tareas que no retienen estado entre invocaciones, 900~segundos (15~min) máximos y 6~MB de carga útil síncrona por solicitud y respuesta~\parencite{awslambda2026quotas}. Azure Functions suma un tope propio de 230~segundos en funciones HTTP, heredado del balanceador de carga, sobre los 5 a 10~minutos del plan de consumo~\parencite{microsoft2026functionsscale}. Cloudflare Workers mide otra magnitud, 128~MB por isolate y tiempo de CPU efectivo, que excluye la espera por red, sin tope de reloj mientras el cliente siga conectado~\parencite{cloudflare2026workerslimits}. El desglose por plataforma está en la sección~\ref{sec:cuadro}. 

Los contenedores sin servidor suben ese techo, Cloud Run admite hasta 60~minutos y 32~GiB por instancia, con el mismo diseño sin estado persistente local~\parencite{gcp2026runtimeout, gcp2026runmemory}. Las instancias siempre encendidas no imponen techo de ejecución ni de payload, a costa de gestión continua. A esos topes se suma una restricción de topología, las funciones no son direccionables en red, así que dos invocaciones concurrentes no comparten estado y deben externalizarlo a un servicio administrado~\parencite{predoaia2024cloudagnostic}. Ahí queda el anclaje, porque portar la aplicación obliga a reescribir su capa de persistencia~\parencite{predoaia2024cloudagnostic, wen2023}. El resto se concentra en las firmas de invocación y en las bibliotecas propietarias del SDK, no en la lógica de negocio~\parencite{rodrigues2022quickfaas}.

Cargas cortas, idempotentes y con estado externalizable caben sin fricción en una función o un contenedor sin servidor, donde la dependencia se paga con velocidad de desarrollo. Procesos largos, con estado voluminoso o payloads mayores obligan a elegir entre instancias siempre encendidas y una plataforma abierta autogestionada (sección~\ref{sec:alternativas}), que devuelve portabilidad a cambio de complejidad operacional. Una capa de abstracción multinube mantiene el código portable a costo medible, la versión agnóstica de una función elevó su arranque en frío en 3,2~\% y 3,8~\% sobre Google Cloud Platform y Azure respectivamente, con memoria equivalente~\parencite{rodrigues2022quickfaas}. Ese margen justifica la abstracción cuando se anticipa migrar; si no, conviene asumir las APIs nativas y aislar la persistencia tras una interfaz propia, dado que la portabilidad de runtime de WASI (sección~\ref{sec:wasm_wasi}) no cubre esa capa.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 3. ANÁLISIS COMPARATIVO DE TECNOLOGÍAS Y PRODUCTOS
%% Entregable de la ficha: cuadro comparativo de al menos 6 plataformas con
%% sus límites técnicos. Aquí se levantan TODOS los precios del informe.
%% IA: prohibida en el análisis comparativo y la justificación de criterios
%% (punto 6.1).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Análisis comparativo de tecnologías y productos}

%% ---------------------------------------------------------------------------
%% 3.1                                          Responsables: Claudio y Daniel
%% Presupuesto: 0,75 página / ~300 palabras + tabla.
%% OBLIGATORIO (punto 4): la fila "Tecnologías, productos y proveedores a
%% comparar" de la ficha tiene CINCO listas que terminan en "y otros que el
%% grupo debe identificar". Por cada lista: al menos DOS alternativas que no
%% figuren, con qué aportan y por qué merecen entrar en la comparación. Si
%% para alguna lista no hay más, declararlo expresamente y documentar la
%% búsqueda (fuentes, criterios de descarte, fecha) -> detalle en Anexo D.
%% Las que califiquen entran como filas de la Tabla 3.2.
%% Listas: FaaS · contenedores sin servidor · cómputo en el borde · código
%% abierto · datos sin servidor.
%% ---------------------------------------------------------------------------
\subsection{Alternativas adicionales}
\label{sec:alternativas}

% Redacción: Daniel (borrador propio, corrección de estilo con IA, nivel 1;
% declarado en Anexo A). Estructura: párrafo 1 método y resultado; párrafo 2
% análisis por dimensiones y filas que pasan a 3.2; tabla con las diez que entran.

La ficha del tema TI-05 \parencite{moya2026indicaciones} pide, en cada una de sus cinco listas, incorporar al menos dos alternativas que no figuren ya entre las plataformas nombradas. Los candidatos se identificaron a partir de las tablas de plataformas de los surveys citados en el informe, del CNCF Landscape y de la documentación oficial de proveedores, con verificación de fuentes el 20 de septiembre de 2026. Se evaluó un conjunto de treinta y tres candidatos con los criterios del Anexo~\ref{anexo:busqueda} (puertas de descarte, inclusión y, cuando hacía falta, desempate). Diez fueron retenidas tras aplicar esos criterios y la regla de desempate (Tabla~\ref{tab:alternativas-adicionales}); el resto se descartó por falta de vigencia o verificabilidad, por no pertenecer al modelo de la lista, por no aportar una dimensión nueva frente a la ficha, por solapamiento o retiro, o por desempate cuando sobraban más de dos. Fuentes, fechas y motivos figuran en ese anexo.

Las que entran abren ejes que las plataformas de la ficha no cubren: facturación en euros y operación en la Unión Europea (Scaleway, en funciones y en contenedores); el mismo motor de funciones en nube gestionada y en código abierto (OCI Functions y Fn Project ---un solo contraste de portabilidad, contado en dos listas); escala a cero solo en el plan gratuito frente a cómputo siempre aprovisionado en planes pagados (Render), útil para el modelo de costos de la sección~\ref{sec:costos}; cómputo en el borde con presencia en Latinoamérica (Azion) y cobro de solicitudes separado del ancho de banda, con tiempos de CPU de decenas de segundos (Bunny); mecanismos de escala a cero elegibles por función en Kubernetes (Fission); y datos con cobro por filas o por comando Redis, más la opción de autoalojar libSQL (Turso y Upstash). De estas diez, Oracle OCI Functions y Azion Edge Functions pasan como filas a las Tablas~\ref{tab:cuadro-3-2a} y~\ref{tab:cuadro-3-2b}, y Fission a la Tabla~\ref{tab:cuadro-3-2c}, con las mismas columnas de límites y precio que el resto..

\tabla{Alternativas adicionales incorporadas a la comparación}{alternativas-adicionales}{|p{3.2cm}|p{3.4cm}|p{7.2cm}|}{%
    \filaTabla{\textbf{Lista de la ficha} & \textbf{Alternativa} & \textbf{Qué aporta a la comparación}}
    \filaTabla{Funciones como servicio & Oracle Cloud Infrastructure Functions & Mismo motor que Fn Project en nube gestionada; invocación desacoplada hasta 3600~s.}
    \filaTabla{Funciones como servicio & Scaleway Serverless Functions & Precio de lista en euros y operación en la Unión Europea; concurrencia uno por instancia.}
    \filaTabla{Contenedores sin servidor & Render & En gratuito se apaga a los 15~min sin tráfico; en pagado el cómputo sigue aprovisionado.}
    \filaTabla{Contenedores sin servidor & Scaleway Serverless Containers & Cobra vCPU-s y GB-s por separado, cada uno con capa gratuita, en la Unión Europea.}
    \filaTabla{Cómputo en el borde & Azion Edge Functions & Presencia documentada en Latinoamérica; capa gratuita de horas de cómputo e invocaciones.}
    \filaTabla{Cómputo en el borde & Bunny Edge Scripting & Solicitudes y CPU cobrados aparte del ancho de banda; hasta 30~s de CPU por solicitud.}
    \filaTabla{Código abierto & Fission & En Kubernetes, el ejecutor define si hay conjunto caliente o escala a cero por función.}
    \filaTabla{Código abierto & Fn Project & Motor de código abierto sobre el que corre OCI Functions (par con la fila gestionada).}
    \filaTabla{Datos sin servidor & Turso (libSQL) & Cobro por filas leídas y escritas; libSQL autoalojable, derivado de SQLite.}
    \filaTabla{Datos sin servidor & Upstash Redis & Redis sin servidor con cobro por comando; ausente de las bases de la ficha.}
}

%% ---------------------------------------------------------------------------
%% 3.2                     Responsables: Valentina (criterios), Rodolfo (AWS y
%%                         Azure; consolida y escribe el análisis), Claudio y
%%                         Daniel (Google, Cloudflare, Fastly, Deno/Vercel/
%%                         Netlify, open source).
%% Presupuesto: 1,25 página / ~250 palabras + tabla (idealmente en una página
%% horizontal: ver \begin{landscape} si hace falta, paquete pdflscape).
%% Criterios: costo, latencia, complejidad operacional (las tres dimensiones
%% del alcance de la ficha), justificados en un párrafo + tabla chica.
%% Cuadro: mínimo 6 plataformas de más de una categoría + las alternativas
%% de 3.1 que califiquen. Columnas de límites y de precio.
%% PRECIOS (punto 5): producto, moneda, región, fecha de consulta, lista o
%% cotización. Verificados en la calculadora oficial. Sin fecha no vale.
%% "Solo por cotización" si el proveedor no publica precio.
\FloatBarrier

%% ---------------------------------------------------------------------------
\subsection{Criterios y cuadro comparativo de plataformas}
\label{sec:cuadro}

%Para evaluar las alternativas tecnológicas frente al modelo de referencia de instancias siempre encendidas, se establecen tres criterios estructurados que capturan las dimensiones técnica, económica y operativa requeridas por el caso de licitación:
Los criterios son los tres que definen el alcance de la ficha (costo, latencia y complejidad operacional) y no hay más: cada columna del cuadro sirve para medir uno de ellos. Ahora bien, no los tres se comparan de la misma manera, y el cuadro lo refleja así. El costo sí puede compararse entre funciones, contenedores sin servidor e instancias siempre encendidas, ya que los tres se cobran por recurso asignado y tiempo consumido; lo que cambia es si el tiempo ocioso también se factura. Bajo esa misma unidad, el precio por duración de una función llega a ser entre 3,5 y 5,6 veces más alto que el de una máquina virtual \parencite{liu2024demystifying}, por lo que ninguna cifra tiene sentido sin especificar su unidad de cobro.

Con la latencia la cosa cambia: no existe una única forma de medirla. Las revisiones bibliográficas dan rangos según el mecanismo de aislamiento \parencite{li2022serverless}, mientras que las evaluaciones experimentales reportan percentiles de cola \parencite{aslanpour2024faashouse}, y estos dos tipos de datos no son convertibles entre sí. Por eso el cuadro se limita a las mediciones propias, hechas en la sección~\ref{sec:poc}, en vez de mezclar metodologías ajenas que no son comparables.

La complejidad operacional, en cambio, ni siquiera cuenta con una métrica estandarizada: los \emph{surveys} la resuelven con etiquetas cualitativas que ni sus propias tablas logran respaldar con datos \parencite{castro2019rise}, y la única operacionalización empírica disponible pasa por indicadores indirectos medidos sobre aplicaciones reales \parencite{eismann2021}. Por eso, en el cuadro, este criterio queda sin puntaje.


\tabla{Criterios de evaluación aplicados al cuadro comparativo}{criterios}{|p{2.8cm}|p{5.4cm}|p{5.6cm}|}{%
    \filaTabla{\textbf{Criterio} & \textbf{Por qué importa} & \textbf{Cómo se mide en el cuadro}}
    \filaTabla{Costo y modelo de cobro & Determina la viabilidad económica en función del tráfico y el riesgo financiero de pisos mínimos o sobrecostos ocultos. & Unidad de cómputo (vCPU-s, GB-s, tiempo de CPU), piso fijo mensual y precio de lista con producto, moneda, región y fecha de consulta; «solo por cotización» cuando el proveedor no publica precio.}
    \filaTabla{Latencia y aislamiento & Condiciona el tiempo de respuesta frente a ráfagas y la frontera de seguridad multi-inquilino entre cargas de trabajo. & Tecnología de aislamiento declarada (microVM, contenedor, isolate V8, Wasm); p50 y p95 de las mediciones propias de la sección~\ref{sec:poc}, o \emph{sin medición}; mitigación del arranque en frío y si se factura aparte.}
    \filaTabla{Complejidad operacional & Cuantifica la carga administrativa y de mantenimiento que queda del lado del equipo en vez del proveedor. & Lista cerrada de responsabilidades, idéntica para todas las filas y elaborada por el grupo, sin escala numérica; límite que fuerza rediseño y necesidad de externalizar el estado.}
}

El análisis comparativo se estructura en doce plataformas representativas que cubren los cuatro modelos de ejecución analizados, complementadas por el panorama de código abierto. Todas las tarifas y cuotas corresponden a documentación oficial consultada el 20 de septiembre de 2026.

\begin{landscape}
\subsubsection*{Tabla 3.2a: Criterios arquitectónicos, operativos y económicos}
\label{tab:cuadro-3-2a}

{\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{longtable}{|p{2.1cm}|p{1.6cm}|p{2.4cm}|p{2.8cm}|p{3.1cm}|p{1.0cm}|p{2.1cm}|p{2.6cm}|p{2.8cm}|}
\hline
\textbf{Plataforma} & \textbf{Modelo} & \textbf{Aislamiento} & \textbf{Unidad de cobro y piso} & \textbf{Precio de lista fechado} & \textbf{Cold start} & \textbf{Mitigación (factura)} & \textbf{Qué opera el equipo} & \textbf{Estado / Límite rediseño} \\ \hline
\endhead

AWS Lambda & FaaS & MicroVM Firecracker (cgroups/chroot por función) & Solicitudes (\$0.20/M) + GB-s reloj. Sin piso; escala a cero. & us-east-1, USD, 20-09-2026: \$0.0000166667/GB-s x86; Free 1M req/400k GB-s. Lista & (4.1) & Concurrencia aprovisionada: SÍ (\$0.0000041667/GB-s). SnapStart sin cargo & runtime, despliegue, observabilidad & SÍ obligatorio (S3/DynamoDB). Límite: 900~s máx, 6~MB payload sync \\ \hline

Azure Functions (Flex Consumption) & FaaS & Contenedor / microVM en entorno aislado por app & Solicitud (\$0.40/M) + GB-s (\$0.000026). Sin piso; escala a cero. & East US, USD, 20-09-2026: \$0.40/M req + \$0.000026/GB-s; Free 250k req / 100k GB-s. Lista & s/m & Instancias Always Ready: SÍ (\$0.000004/GB-s base + \$0.000016 ejec) & runtime, despliegue, observabilidad & SÍ obligatorio (Blob/Cosmos). Límite: HTTP máx 230~s (Load Balancer) \\ \hline

Google Cloud Run functions & FaaS & gVisor (gen 1) o microVM (gen 2) sobre Cloud Run & vCPU-s, GiB-s y req. Sin piso; escala a cero. Cloud Build/Artifact aparte. & us-east4 (Tier 1), USD, 20-09-2026: CPU \$0.000024/s, RAM \$0.0000025/s, req \$0.40/M. Free tier Tier 1. Lista & s/m & Min instances y Startup CPU boost: SÍ se facturan al arranque & runtime, despliegue, observabilidad & SÍ obligatorio. Límite: 9~min en eventos frente a 60~min en HTTP \\ \hline

Google Cloud Run (servicios) & Contenedor & Sandbox gVisor (gen 1) o microVM Linux (gen 2) & vCPU-s y GiB-s (100~ms) + req. Sin piso mensual; escala a cero. & us-east4 (Tier 1), USD, 20-09-2026: CPU \$0.000024/s, idle \$0.0000025/s, RAM \$0.0000025/s. Lista & (4.1) & Min instances (idle) y CPU boost: SÍ se facturan & runtime, red (VPC/SSL), despliegue, observabilidad & SÍ (disco en RAM). Límite: 60~min máx; tareas largas van a Jobs (168~h) \\ \hline

AWS Fargate (ECS) & Contenedor & MicroVM dedicada por tarea (Firecracker en AWS) & vCPU-h y GB-h por segundo (mín 1~min). Sin piso; escala a cero. & us-east-1, USD, 20-09-2026: \$0.04048/vCPU-h, \$0.004445/GB-h. Fargate Spot desc. 70\%. Lista & s/m & No aplica a nivel función: mantener tareas mínimas activas completas & runtime, red (VPC/ALB), despliegue, observabilidad & Efímero 20~GB; persistencia vía EFS/EBS. Sin timeout de plataforma \\ \hline

Azure Container Apps & Contenedor & Contenedor en clúster K8s/Envoy dedicado por entorno & vCPU-s y GiB-s activos. Sin piso; escala a cero. & East US, USD, 20-09-2026: vCPU \$0.000024/s, idle \$0.000003/s, RAM \$0.000003/s. Free 180k vCPU-s. Lista & s/m & Min replicas en reposo: SÍ se facturan a tarifa idle reducida & runtime, red (VNet), despliegue, observabilidad & SÍ (Azure Files). Límite: 300~s máx en HTTP; batch requiere Jobs \\ \hline

Cloudflare Workers (Paid) & Borde & Isolates V8 multi-tenant en proceso único (espacio virtual común) & Solicitudes (\$0.30/M) + tiempo CPU (\$0.02/M ms). \textbf{Piso: \$5.00/mes cuenta.} & global, USD, 20-09-2026: \$5/mes base (10M req, 30M CPU-ms); reloj sin cobro. Free 100k req/d. Lista & (4.1) & Estructural: isolate en proceso vivo ($<$5~ms); snapshot Wasm. Sin SKU aparte & runtime, despliegue, observabilidad & SÍ obligatorio (DO, KV, D1, R2). Límite: 128~MB RAM y 5~min CPU máx \\ \hline

Fastly Compute & Borde & WebAssembly (WASI) sobre Wasmtime; sandbox por req. & Requests (\$0.50--0.20/M) + vCPU-ms (\$0.05--0.02/M ms). Sin piso. & global, USD, 20-09-2026: tramos uso actual. Free 10M req, 100M ms. Tráfico entrega aparte. Lista & s/m & Estructural: instanciación Wasm en microsegundos. Sin SKU aparte & runtime, despliegue, observabilidad & SÍ (Config/KV/Secret Store). Límite: 50~ms CPU por req., 128~MB heap \\ \hline

AWS Lambda@Edge / CloudFront & Borde & L@E: microVM en PoP edge; CF Functions: proceso liviano JS & L@E: \$0.60/M + \$0.00005001/GB-s. CF Func: \$0.10/M (2M free). Sin piso. & global, USD, 20-09-2026: precios de lista publicados en CDN CloudFront & s/m & CF Func submilisegundo estructural. L@E sin concurrencia aprovisionada & runtime, despliegue (asociación a CDN) & SÍ (sin disco local). Límite: CF Func 1~ms CPU; L@E 5~s viewer / 30~s origin \\ \hline

EC2 reservada (ECS t3.medium) & Siempre encendido & VM dedicada Nitro/KVM; sin aislamiento por invocación & Instancia-hora. \textbf{Piso: mes completo (\$30.37/mes On-Demand). No escala a cero.} & us-east-1, USD, 20-09-2026: On-Demand \$0.0416/h; Reserva 1a \$0.026/h (\$18.98/m); 3a \$0.017/h (\$12.41/m). & n/a & No aplica: proceso residente en memoria absorbe tráfico de inmediato & SO/host, Docker, capacidad, escalado, red, despliegue, observabilidad & Estado en memoria/disco host (EBS). Sin timeout de plataforma \\ \hline

Oracle OCI Functions (3.1) & FaaS & Contenedor gestionado / microVM OCI (motor Fn Project) & Invocaciones (\$0.20/M) + GB-s (\$0.00001417/s). Concurrency idle 25\%. Sin piso. & US East, USD, 20-09-2026: Always Free 2M req + 400k GB-s/mes permanentes. Lista & s/m & Concurrencia aprovisionada: SÍ (25\% tarifa en idle; sin recargo activo) & runtime, despliegue, observabilidad & SÍ (Object Storage, DB). Límite: 300~s sync; modo detached hasta 3600~s \\ \hline

Azion Edge Functions (3.1) & Borde & Isolates V8 sobre Edge Runtime distribuido propio & Compute (\$0.072/1000~s CPU) + req (\$0.30/M). Sin piso en Starter. & Brasil/LATAM/global, USD, 20-09-2026: Free 8~h compute + 3M req/mes. Lista & s/m & Aislamiento liviano V8 ($<$2~s cold start doc, típico ms). Sin SKU & runtime, despliegue, observabilidad & SÍ (Edge Storage/SQL). Límite: \textbf{2~s CPU por req} (wall 5~min); isolate 512~MB \\ \hline
\end{longtable}
}

\clearpage

\subsubsection*{Tabla 3.2b: Límites técnicos y tarifas de egreso de datos}
\label{tab:cuadro-3-2b}

{\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{longtable}{|p{2.6cm}|p{3.2cm}|p{3.4cm}|p{3.2cm}|p{3.6cm}|p{4.2cm}|}
\hline
\textbf{Plataforma} & \textbf{Tiempo máx. ejecución} & \textbf{Memoria / vCPU} & \textbf{Payload in / out} & \textbf{Concurrencia / escalado} & \textbf{Egreso de datos (precio fechado)} \\ \hline
\endhead

AWS Lambda & 900~s (15~min) & 128 a 10.240~MB (1~vCPU a 1.769~MB, máx 6~vCPU) & Sync: 6~MB. Async: 256~KB & 1.000 concurrentes regional default (ampliable). Escala a cero & 100~GB/mes gratis; luego \$0.09/GB (primeros 10~TB us-east-1). 20-09-2026 \\ \hline

Azure Functions (Flex) & HTTP: 230~s (techo Load Balancer); otros triggers 30~min (máx unbounded) & 512~MB (0.5~vCPU), 2048~MB (1~vCPU), 4096~MB (2~vCPU) & HTTP 100~MB en gateway; Storage Queues 4~MB & Hasta 1.000 instancias por app. Escala a cero & 100~GB/mes gratis; luego \$0.087/GB (East US). 20-09-2026 \\ \hline

Google Cloud Run functions & HTTP hasta 60~min; eventos hasta 9~min (API v2) & Default 256~MiB; máx 16~GiB / 4~vCPU según tabla funciones & HTTP/1 32~MiB req/resp. Sin límite bajo HTTP/2 & Hasta 1.000 req concurrentes por instancia. Escala a cero & Premium Tier: 1~GiB gratis; luego \$0.12/GB hacia Norteamérica. 20-09-2026 \\ \hline

Google Cloud Run (servicios) & 300~s default; máx 3600~s (60~min). Jobs 168~h & Default 512~MiB (mín 128~MiB, máx 32~GiB); vCPU default 1 (0.08 a 8) & HTTP/1 32~MiB. Sin límite con streaming HTTP/2 & Instancia: default 80, máx 1.000. Revisión: máx 100 inst. default. Escala a cero & Premium Tier: 1~GiB gratis; luego \$0.12/GB (1--1.024~GiB). Interno sin cargo. 20-09-2026 \\ \hline

AWS Fargate (ECS) & Sin límite de plataforma (proceso continuo) & 0.25~vCPU (0.5--2~GB) a 32~vCPU (hasta 244~GB en doc ECS) & Sin límite de plataforma (ALB máx 100~MB cuerpo) & Cuota default 1.000 tareas regional. Escala a cero si desired=0 & Salida vía NAT Gateway (\$0.045/GB + \$0.045/h) + egreso EC2 (\$0.09/GB). 20-09-2026 \\ \hline

Azure Container Apps & HTTP 300~s máx; Jobs sin límite (hasta 7 días) & 0.25~vCPU / 0.5~GiB a 4.0~vCPU / 8.0~GiB en Consumption & HTTP hasta 100~MB soportados vía ingress Envoy & Hasta 300 réplicas por app (default 10--30). Escala a cero & 100~GB/mes gratis; luego \$0.087/GB hacia internet. 20-09-2026 \\ \hline

Cloudflare Workers (Paid) & CPU: 5~min máx (\textbf{30~s default}). Reloj: \textbf{SIN LÍMITE} en HTTP & 128~MB por isolate, NO configurable. vCPU no expuesta & URL 16~KB; cabeceras 128~KB; body req según plan (100~MB a 5~GB) & Sin límite numérico publicado; 10.000 subsolicitudes en Paid. Escala a cero & \textbf{SIN CARGO declarado:} egreso y ancho de banda incluidos en Workers Paid. 20-09-2026 \\ \hline

Fastly Compute & CPU: \textbf{50~ms por ejecución}. Reloj: 2~min (60~s trial) & 1~MB stack y 128~MB heap por ejecución, no configurable & Cabeceras 128~KB; URL 8~KB; caché 100~MB; paquete Wasm 100~MB & Instancia aislada por req. Encadenamiento máx 20 saltos y 6 servicios & \textbf{Entrega (Full Site Delivery) aparte:} 100~GB free; luego \$0.12/GB (US/EU), \$0.19 (LATAM). 20-09-2026 \\ \hline

AWS Lambda@Edge / CF Func & CF Func: \textbf{1~ms CPU}. L@E: 5~s viewer / 30~s origin & CF Func: 2~MB fijos. L@E: 128~MB (viewer) o 128--10.240~MB (origin) & CF Func: sin cuerpo. L@E: 40~KB viewer, 1~MB origin (40~MB stream) & Integrado en escala CDN global CloudFront. Escala a cero & Tarifa CloudFront: 1~TB/mes gratis; luego \$0.085/GB primer tramo en Norteamérica. 20-09-2026 \\ \hline

EC2 reservada (ECS t3.medium) & Sin límite de plataforma (ejecución continua) & Fijo de instancia: 2~vCPU y 4.0~GiB de RAM dedicados & Sin límite de plataforma; gobernado por memoria de host y proxy & Capacidad física de t3.medium; autoescalado por ASG (no a cero) & Salida estándar EC2: 100~GB/mes gratis; luego \$0.09/GB (us-east-1). 20-09-2026 \\ \hline

Oracle OCI Functions (3.1) & Sync: 300~s (\textbf{default 30~s}). Detached: 5 a \textbf{3.600~s (60~min)} & Opciones fijas: 128, 256, 512, 1024, 2048 y 3072~MB & Solicitud y respuesta HTTP: 6~MB cada una & 60 concurrentes por AD default (ampliable). Escala a cero & \textbf{Primeros 10~TB/mes gratis hacia internet}; luego \$0.0085/GB. 20-09-2026 \\ \hline

Azion Edge Functions (3.1) & CPU: \textbf{2~s máx por req}. Reloj wall-clock: máx \textbf{5~min (300~s)} & Hasta 512~MB por isolate de función. vCPU no expuesta & Args JSON 100~KB. Código: UI 6~MB, API 20~MB. Sub-requests: 50 & Autoescalado sobre red distribuida de PoPs edge. Escala a cero & Red Edge por GB (Brasil/LATAM \$0.06/GB en inicio; 50~GB gratis). 20-09-2026 \\ \hline
\end{longtable}
}
\end{landscape}

\subsubsection*{Tabla 3.2c: Ecosistema de plataformas de código abierto}
\label{tab:cuadro-3-2c}

{\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{longtable}{|p{2.3cm}|p{2.4cm}|p{2.2cm}|p{1.3cm}|p{3.6cm}|p{2.7cm}|}
\hline
\textbf{Proyecto} & \textbf{Modelo} & \textbf{Aislamiento} & \textbf{Escala 0 default} & \textbf{Defaults configurables (timeout / concurrencia)} & \textbf{Acreditación CNCF u otra} \\ \hline
\endhead

Knative Serving & Contenedores K8s & Contenedor (pod K8s) & \textbf{Sí (30~s gracia)} & Timeout 300~s default (máx 600~s). Concurrencia 0 (ilimitada default, máx 1000) & \textbf{Graduado CNCF (11-09-2025)}. Categoría: Scheduling \& Orchestration \\ \hline

OpenFaaS & FaaS sobre K8s / host & Contenedor (gVisor opcional en Pro) & No (opt-in) & Timeouts en gateway y función configurables. JetStream ack\_wait 60~s. Soporta \texttt{faasd} sin K8s & \textbf{No es CNCF}. OpenFaaS Ltd. Licencias CE (EULA) y comercial Pro/Edge \\ \hline

KEDA & Autoescalador K8s & No aplica (escala otros) & \textbf{Sí} & No aplica en ejecución HTTP. Sondeo default 30~s (\texttt{pollingInterval}) & \textbf{Graduado CNCF (22-08-2023)}. Scheduling \& Orchestration \\ \hline

Fermyon SpinKube & Serverless Wasm en K8s & WebAssembly (\texttt{containerd-shim-spin}) & No nativo (delega KEDA) & Timeouts gobernados por pod de K8s y triggers de Spin. Wasm en nodo sin contenedor & \textbf{Sandbox CNCF (21-01-2025)}. Runtime Spin en org \texttt{spinframework} \\ \hline

wasmCloud & Componentes Wasm & WebAssembly (Wasmtime Component Model) & \textbf{Sí (por diseño)} & Concurrencia ilimitada default (\texttt{wash scale}). Control timeout 2000~ms default & \textbf{Incubating CNCF (08-11-2024)}. Scheduling \& Orchestration \\ \hline

Fission (3.1) & FaaS sobre K8s & Contenedor (pods precalentados o dedicados) & Configurable por función & Router timeout 300~s default. Warm pools (\texttt{poolmgr}, 3 pods) vs escala a cero (\texttt{newdeploy}) & \textbf{No es CNCF}. Proyecto comunitario activo (v1.27.0, 2026) \\ \hline
\end{longtable}
}

\subsubsection*{Notas metodológicas y de trazabilidad al pie de las tablas}
\begin{itemize}
    \item \textbf{Nota (a) --- Complejidad operacional («Qué opera el equipo»):} La lista cerrada corresponde a la matriz estandarizada construida por el grupo a partir de las matrices de responsabilidad de AWS, Azure y Google Cloud, y el Whitepaper de Serverless del CNCF. Los proveedores de borde no publican una matriz formal; las responsabilidades son una inferencia técnica documentada desde sus contratos de arquitectura.
    \item \textbf{Nota (b) --- Arranque en frío:} La marca \texttt{(4.1)} remite a los ensayos de laboratorio del grupo (Vicente Arratia). Para las demás plataformas se declara \texttt{sin medición propia} (\texttt{s/m}) para no distorsionar la comparación con benchmarks heterogéneos de proveedores o literatura. La instancia siempre encendida declara \texttt{no aplica} (\texttt{n/a}).
    \item \textbf{Nota (c) --- Lambda@Edge y CloudFront Functions:} Se consolidan en fila conjunta por representar la oferta escalonada de AWS sobre CDN, diferenciando manipulación rápida de cabeceras (1~ms CPU) de cómputo regional (hasta 30~s).
    \item \textbf{Nota (d) --- Respaldo teórico del aislamiento en el borde:} Cloudflare Workers y Fastly Compute utilizan aislamiento a nivel de lenguaje (\emph{V8 Isolates} y \emph{WebAssembly/WASI}) en un mismo espacio de memoria virtual para ofrecer arranque sub-milisegundo. Como demuestran Schwarzl et al.\ \parencite{schwarzl2022spectre} en \emph{Robust and Scalable Process Isolation against Spectre in the Cloud}, este enfoque optimiza la densidad a costa de exposición a canales laterales microarquitectónicos (\emph{Spectre-PHT}), lo que obliga a incorporar defensas probabilísticas activas (\emph{DyPrIs}).
\end{itemize}

%% ---------------------------------------------------------------------------
%% Análisis comparativo de plataformas (Sección 3.2)
%% ---------------------------------------------------------------------------
El análisis transversal del cuadro comparativo revela que la promesa teórica de «escala a cero y pago estricto por uso» experimenta tensiones significativas cuando se confronta con las exigencias operativas reales. En primer lugar, la mitigación de arranques en frío en FaaS de propósito general introduce costos fijos encubiertos: tanto \emph{Provisioned Concurrency} en AWS Lambda (\$0,0000041667/GB-s reservado más \$0,0000097222/GB-s activo) como las instancias \emph{Always Ready} en Azure Functions Flex (\$0,000004/GB-s de reserva continua) transforman la facturación variable en un piso mensual análogo al de una instancia aprovisionada. En cargas con tráfico sostenido o ráfagas regulares (como la recepción continua de telemetría de grúas y contenedores en TERABYTE), este sobrecosto aproxima el gasto mensual al punto de equilibrio de una instancia dedicada reservada (EC2 \texttt{t3.medium} a \$18,98/mes, Fila~10), donde el proceso residente en memoria elimina el retardo por diseño. Asimismo, el cómputo en el borde evidencia una bifurcación económica y técnica crítica: ejecutar lógica en la periferia mediante microVMs regionales (AWS Lambda@Edge) impone un costo de cómputo de \$0,00005001/GB-s, exactamente 3,0 veces superior al de Lambda estándar (\$0,0000166667/GB-s), además de carecer de capa gratuita y trasladar tarifas de distribución CDN sin posibilidad de concurrencia aprovisionada. En contraste, las soluciones basadas en aislamiento a nivel de lenguaje (Cloudflare Workers y Fastly Compute) eliminan el arranque en frío mediante procesos e isolates compartidos sub-milisegundo, pero exigen una renuncia arquitectónica severa: operan bajo estrictos límites de CPU (50~ms por solicitud en Fastly y 2~s en Azion), carecen de sistemas de archivos convencionales y trasladan la persistencia a bases externas mediante llamadas de red adicionales.

En segundo lugar, la complejidad operacional no desaparece en las arquitecturas serverless, sino que se redistribuye entre la infraestructura y el diseño de la aplicación. La transición desde FaaS hacia contenedores sin servidor (AWS Fargate y Azure Container Apps) amplía la flexibilidad de empaquetado y elimina las cuotas de tiempo (soportando procesos batch continuos o tareas de hasta 7 días), pero transfiere al equipo responsabilidades de ingeniería de red considerables: requiere provisionar subredes VPC privadas, balanceadores de carga de aplicación (ALB) y pasarelas NAT (cuyo costo base de \$0,045/hora más \$0,045/GB suele exceder el cómputo en volúmenes medios), además de sintonizar manualmente políticas de autoescalado (KEDA o métricas de CPU). Esta fricción se acentúa por límites duros no configurables de la infraestructura subyacente que contradicen las cuotas promocionadas por los proveedores: en Azure Functions, si bien la plataforma publicita tiempos de ejecución de hasta 60~minutos o modo no acotado (\emph{unbounded}), las funciones disparadas por HTTP enfrentan un techo infranqueable de 230~segundos impuesto por el \emph{idle timeout} del Azure Load Balancer, obligando a rediseñar procesos analíticos hacia patrones asíncronos desacoplados mediante colas o \emph{Durable Functions}. Finalmente, el ecosistema serverless introduce un riesgo latente de dependencia y obsolescencia que valida la necesidad de evaluar alternativas portables (Sección~\ref{sec:alternativas}): el anuncio oficial de AWS cerrando App Runner a nuevos clientes en 2026 demuestra que las abstracciones gestionadas de alta conveniencia están sujetas a discontinuación unilateral del proveedor, consolidando a las plataformas basadas en contenedores abiertos y estándares de la CNCF (Knative, Fn Project, Fission) como salvaguardas indispensables para la soberanía arquitectónica institucional.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 4. CASO DE ESTUDIO E IMPLEMENTACIÓN PRÁCTICA
%% Dos entregables de la ficha (PoC y modelo de costos) más la aplicación al
%% caso de licitación. Los tres comparten escenario: la PoC corre en
%% plataformas que están en la Tabla 3.2; el modelo cotiza una de esas
%% plataformas con los precios de la Tabla 3.2; el caso TERABYTE usa el
%% punto de equilibrio de 4.2 y la latencia de 4.1.
%% Esto es el "caso práctico" del 70 % de la nota.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Caso de estudio e implementación práctica}

%% ---------------------------------------------------------------------------
%% 4.1                                                    Responsable: Vicente
%% Presupuesto: 1,25 página / ~350 palabras + 1 gráfico + 1 tabla resumen.
%% Ficha: "prueba de concepto documentada (código y mediciones) de arranque
%% en frío y latencia, O BIEN análisis de mediciones publicadas con su
%% metodología". Se hace la propia: función trivial en 2 o 3 plataformas de
%% la Tabla 3.2 (Lambda, Cloud Run, Cloudflare Workers; free tier), script
%% que mide cold start vs. warm, N invocaciones, misma región si es posible.
%% En el cuerpo: metodología (1 párrafo), gráfico, tabla resumen (p50/p95
%% cold y warm por plataforma), interpretación y limitaciones.
%% Código, paso a paso y datos crudos -> Anexo B. Código auxiliar generado
%% con IA se identifica como tal y se declara.
%% ---------------------------------------------------------------------------
\subsection{Prueba de concepto: arranque en frío y latencia}
\label{sec:poc}

Se desplegó una función trivial idéntica, que devuelve el identificador de la
instancia que responde y su tiempo de vida, en tres plataformas de la
Tabla~\ref{tab:cuadro-3-2a}: AWS Lambda (microVM, us-east-1, 128~MB), Google
Cloud Run (contenedor, us-east4, 256~MiB) y Cloudflare Workers (V8 isolate, borde,
128~MB). Desde un cliente en Chile se corrieron 25 ciclos por plataforma en la misma
ventana (21-09-2026): una petición tras inducir el arranque en frío y cinco
calientes, cada una con conexión TCP/TLS nueva. El frío se induce con el evento del
ciclo de vida propio de cada modelo (cambio de configuración, publicación de versión,
terminación del proceso), de modo que la petición medida llega sin instancia caliente
disponible; la clasificación frío/caliente usa lo que devuelve la función, no la
latencia. Se reportan la latencia total percibida y la penalización de arranque (p50
frío menos p50 caliente), que cancela la red. Código, datos y notas en el Anexo~B.

\imagen{assets/images/poc-coldstart.pdf}{Latencia total desde el cliente en arranque en frío y en caliente, por plataforma (25 ciclos forzados, 150 peticiones por plataforma, 21-09-2026). La caja cubre el rango intercuartílico, la línea es la mediana y los puntos son valores atípicos.}{poc}

\tabla{Resumen de mediciones de la prueba de concepto (latencia total desde el cliente, ms)}{poc-resumen}{|p{3cm}|p{2cm}|p{2cm}|p{2.1cm}|p{2.1cm}|p{3.2cm}|}{%
    \filaTabla{\textbf{Plataforma} & \textbf{Frío p50} & \textbf{Frío p95} & \textbf{Caliente p50} & \textbf{Caliente p95} & \textbf{n frío/caliente, región, fecha}}
    \filaTabla{AWS Lambda & 838 & 890 & 442 & 507 & 25/125, us-east-1, 21-09-2026}
    \filaTabla{Cloudflare Workers & 569 & 591 & 567 & 574 & 107/43, borde (colo GIG), 21-09-2026}
    \filaTabla{Google Cloud Run & 922 & 1\,656 & 203 & 358 & 24/125, us-east4, 21-09-2026}
}

%% ---------------------------------------------------------------------------
%% [REDACTAR] Interpretación (~140 palabras, autoría propia).
%% Hechos disponibles, en el orden sugerido:
%%  1. Penalización p50: Lambda +395 ms, Workers +1 ms, Cloud Run +720 ms
%%     (results/resumen.csv). El orden sigue al tipo de aislamiento:
%%     isolate < microVM < contenedor. Es hallazgo propio.
%%  2. En Workers el arranque existe (107 isolates recién creados declararon
%%     first_request) pero el cliente no lo percibe: la latencia es la del
%%     viaje al borde. En Lambda los 25 fríos cayeron exactamente en la primera
%%     petición de cada ciclo.
%%  3. En caliente el orden se invierte y lo manda la red, no el modelo:
%%     Google termina la conexión TCP/TLS en un punto de presencia en Chile
%%     (TCP 16 ms) y reenvía por red interna a us-east4; Lambda conecta directo
%%     a Virginia (TCP ~140 ms). La latencia total incluye la arquitectura de
%%     front-end de cada proveedor.
%%  4. Cloud Run es la más dispersa (306-1682 ms): al terminar el proceso la
%%     plataforma empieza a reponer el contenedor por su cuenta y la petición
%%     paga la parte del arranque que falta cuando llega. El arranque del
%%     contenedor que registra la propia plataforma es parejo (~1,0 s).
%%  5. Cierre posible: la sección 2.2 desarrolla causas y mitigaciones; estas
%%     mediciones son la línea base sin mitigación (sin concurrencia
%%     aprovisionada, sin CPU boost, sin instancias mínimas).
%% ---------------------------------------------------------------------------
\textbf{[REDACTAR: interpretación]}

%% ---------------------------------------------------------------------------
%% [REDACTAR] Limitaciones (~80 palabras, autoría propia). Cuatro hechos:
%%  a. Cloud Run corre con 256 MiB porque Node.js 24 no cabe en 128 MiB en esa
%%     plataforma (el contenedor moría por memoria entre peticiones); Workers
%%     tiene 128 MB fijos y Lambda queda en 128 MB. En Lambda la CPU es
%%     proporcional a la memoria; en Cloud Run se fija aparte (1 vCPU).
%%  b. Los tres mecanismos para inducir el frío son distintos porque los tres
%%     ciclos de vida lo son; el de Cloud Run (terminar el proceso) es
%%     instrumentación de la prueba, no comportamiento de producción. Crear
%%     una revisión nueva no sirve: la plataforma la valida arrancando un
%%     contenedor antes de enrutar tráfico (24/25 primeras peticiones llegaron
%%     a un contenedor ya despierto en una corrida previa de la misma noche).
%%  c. La latencia al borde depende de la ruta del ISP: el 19-09 Workers
%%     caliente dio 231 ms y el 21-09, 567 ms, con el mismo colo (GIG). Dentro
%%     de una misma ventana la comparación entre plataformas sí es válida.
%%  d. Función trivial y un solo cliente: no representa cargas reales ni
%%     concurrencia.
%%  Claves citables (ya en referencias.bib, verificadas 21-09-2026): cloudflare2026workerslimits
%%  (128 MB fijos), awsLambdaMemory (CPU proporcional a memoria), gcrCpu (vCPU aparte;
%%  startup CPU boost), gcprunenv (gen1/gen2), gcrHealthchecks (sonda de arranque).
%% ---------------------------------------------------------------------------
\textbf{[REDACTAR: limitaciones]}

%% ---------------------------------------------------------------------------
%% 4.2                                                    Responsable: Francisca
%% Presupuesto: 1 página / ~300 palabras + tabla de supuestos + 1 gráfico.
%% Ficha: "modelo de costos que compare una arquitectura sin servidor con una
%% de contenedores siempre encendidos, identificando el volumen de
%% equilibrio en solicitudes por mes".
%% En el cuerpo: supuestos (duración media, memoria, egress, tamaño de
%% respuesta, escenarios de carga), gráfico costo mensual vs. solicitudes/mes
%% con las dos curvas y el cruce, el número de equilibrio.
%% Planilla completa -> Anexo C. Precios: solo los de la Tabla 3.2.
%% ---------------------------------------------------------------------------
\subsection{Modelo de costos y volumen de equilibrio}
\label{sec:costos}

El modelo compara AWS Lambda con contenedores siempre encendidos (ECS sobre instancias EC2 t3.medium), el comparador fijo del cuadro de la sección 3.2. Usa solo los precios de las Tablas 3.2a y 3.2b (us-east-1, USD, precio de lista consultado el 20/09/2026). Con $N$ solicitudes al mes, memoria $m$ (GB), duración $d$ (s) y con $q_{\mathrm{pico}}$ el pico en solicitudes por segundo, Lambda cuesta $C_L=\max(0,N-10^{6})\,c_r+\max(0,\,N\,m\,d-4\cdot10^{5})\,c_g$, con $c_r=0{,}20$ USD por millón de solicitudes y $c_g=0{,}0000166667$ USD por GB-s. La flota fija cuesta $C_F=k\,p\cdot730$, con $p=0{,}026$ USD por hora y $k=\max(2,\lceil q_{\mathrm{pico}}/20\rceil)$ instancias. La Tabla~\ref{tab:supuestos-42} declara los supuestos.

\begin{table}[htbp]
\centering
\caption{Supuestos del modelo de costos}
\label{tab:supuestos-42}
{\scriptsize
\begin{tabular}{|p{0.17\linewidth}|p{0.56\linewidth}|p{0.17\linewidth}|}
\hline
\textbf{Supuesto} & \textbf{Valor} & \textbf{Origen} \\ \hline
Carga & Solicitudes HTTP tipo API, 730 h al mes; pico igual a 4 veces el promedio & Supuesto \\ \hline
Sin servidor & AWS Lambda x86, 512 MB y 200 ms por solicitud; 0,20 USD por millón de solicitudes y 0,0000166667 USD/GB-s; capa gratuita de 1 M de solicitudes y 400.000 GB-s & Tabla 3.2a \\ \hline
Siempre encendido & ECS sobre EC2 \texttt{t3.medium} (2 vCPU, 4 GiB), mínimo 2 instancias, reserva de 1 año a 0,026 USD/h (18,98 USD al mes cada una) & Tabla 3.2a \\ \hline
Capacidad & 20 solicitudes sostenidas por instancia (20 ms de CPU por solicitud = 0,4 vCPU, el 20\% de los 2 vCPU de la instancia) & Supuesto \\ \hline
Excluido & Egreso de datos (misma tarifa en ambos lados, Tabla~3.2b), balanceador, API Gateway, NAT, registros y costo de operar la flota & Declarado \\ \hline
\end{tabular}
}
\end{table}

Con esos supuestos, Lambda es más barata hasta $N^{*} = 24{,}0$ millones de solicitudes al mes (unas 9 por segundo en promedio), donde iguala los 37,96 USD mensuales del par de instancias (Figura~\ref{fig:equilibrio}). Sobre ese volumen la ventaja cambia de signo cada vez que el pico exige otra instancia, y desde unos 54,5 millones la flota fija es siempre más barata. La causa es la distinción de la sección 2.3 entre tiempo de reloj y de CPU \cite{lin_demystifying_2026}. Lambda factura los 200 ms completos de cada solicitud. La instancia, con 20 ms de CPU por solicitud (supuesto), reparte la espera entre solicitudes concurrentes.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=\linewidth, height=6.2cm,
    xlabel={Solicitudes por mes (millones)},
    ylabel={Costo mensual (USD)},
    xmin=0, xmax=60, ymin=0, ymax=115,
    xtick distance=10, ytick distance=20,
    grid=both, minor tick num=1,
    legend pos=north west, legend style={font=\scriptsize},
    tick label style={font=\scriptsize}, label style={font=\small}]
\addplot[thick, blue!70!black] coordinates {(0,0) (1,0) (4,0.6) (60,105.13)};
\addlegendentry{AWS Lambda (512 MB, 200 ms)}
\addplot[thick, dashed, red!75!black] coordinates {(0,37.96) (26.28,37.96) (26.28,56.94) (39.42,56.94) (39.42,75.92) (52.56,75.92) (52.56,94.90) (60,94.90)};
\addlegendentry{2 instancias \texttt{t3.medium} (1 año); cada escalón suma una}
\addplot[only marks, mark=*, mark size=2pt, black] coordinates {(24.01,37.96) (54.52,94.90)};
\node[anchor=south east, font=\scriptsize] at (axis cs:24,41) {$N^{*}\approx24{,}0$ M};
\node[anchor=north west, font=\scriptsize] at (axis cs:55,91) {$54{,}5$ M};
\end{axis}
\end{tikzpicture}
\caption{Costo mensual en función del volumen de solicitudes: serverless frente a contenedores siempre encendidos (escenario base). Elaboración propia con precios de la Tabla 3.2a.}
\label{fig:equilibrio}
\end{figure}

El resultado es sensible al escenario (Anexo C). Con 1 GB y 500 ms el equilibrio baja a 5,3 millones; con reserva a tres años, a 17,0 millones; y con 256 MB y hasta 200 ms, instancias bajo demanda o picos de ocho veces el promedio, Lambda no llega a cruzar. El modelo excluye balanceador, API Gateway, NAT y el costo de operar la flota, cuyos efectos van en ambos sentidos \cite{hamza2024costdynamics}. Por eso el volumen de equilibrio debe leerse como un orden de magnitud, condicionado a los supuestos declarados.

%% ---------------------------------------------------------------------------
%% 4.3                                                    Responsable: Fabián
%% Presupuesto: 0,75 página / ~350 palabras.
%% "Aplicación concreta al caso de licitación del propio grupo" (punto 4, lo
%% que se valora) + "Vínculo con la propuesta técnico-económica" de la ficha:
%% paso de CAPEX a OPEX puro y el riesgo de una factura que crece con el
%% éxito del sistema. Usar el volumen estimado del proyecto TERABYTE, el
%% punto de equilibrio de 4.2 y la latencia de 4.1 para decir qué modelo
%% conviene y cómo se refleja en la propuesta. No redescribir el proyecto.
%% ---------------------------------------------------------------------------
\subsection{Aplicación al caso de licitación de TERABYTE}
\label{sec:caso}

El terminal ya opera 972.000 movimientos de grúa y 1.290.000 de patio al año, más 1.450 camiones diarios con picos de 2.600. Contando registro, confirmación y evidencia por cada evento físico, el tráfico del núcleo transaccional se ubica en el orden de un par de millones de solicitudes mensuales, muy por debajo de los 24,0 millones que fijan el equilibrio de Lambda frente a una flota reservada (\ref{sec:costos}). En pura aritmética de facturación, serverless gana con holgura hoy y seguirá ganando tras crecer a 920.000 TEU en tres años. El costo no cierra la decisión. La prueba de concepto mide 395~ms de penalización mediana por arranque frío en Lambda frente a 1~ms en un borde tipo Workers (\ref{sec:poc}), y el caso impone una cota más dura: la cabina de grúa resuelve el estado en menos de un segundo, sin menús ni confirmaciones, mientras las pilas de contenedores generan jaulas de Faraday que alteran la cobertura del patio cada hora. Sumada a la autonomía exigida de 72~horas sin enlace hacia la nube, esa combinación descarta un núcleo dependiente de invocaciones remotas facturadas por evento. La propuesta técnica del grupo responde fijando el núcleo operacional, el gate y el patio sobre cómputo local siempre encendido y contenedores gestionados, y reserva el borde serverless para el portal público y las notificaciones, donde la continuidad offline no aplica; el patrón replica el monitoreo de cadena de frío ya validado en puertos \parencite{kjorveziroski2021iot}. Esa elección fija también el perfil financiero de la propuesta. Terabyte deja de cotizar CAPEX de servidores propios y pasa a OPEX íntegro, capacidad reservada más nodos locales, de consumo acotado y no proporcional a cada movimiento facturable. Un núcleo puramente serverless habría atado la factura al éxito comercial del terminal, a más TEU y más mensajería EDIFACT exigida por la alianza naviera desde 2029: el riesgo de \textit{expense explosion} que documenta la literatura de precios de FaaS \parencite{liu2024demystifying}. El modelo resultante, coherente con \ref{sec:limites_dependencias} y \ref{sec:cuadro}, conviene al caso, la porción crítica se paga como flota reservada previsible, y solo el tráfico externo y tolerante a arranque en frío se traslada al proveedor por consumo.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> 5. TENDENCIAS Y RIESGOS                            Responsable: Fabián
%% Presupuesto: 1 página / ~450 palabras.
%% Sección exigida por las Pautas del curso ("Tendencias y evolución
%% futura"), escrita con el criterio de las Indicaciones ("evidencia
%% contradictoria bien tratada"). Cada tendencia va con su contraevidencia.
%% Máximo dos casos públicos de equipos que abandonaron serverless por costo
%% o complejidad, verificados en la fuente original (blogs de ingeniería:
%% admisibles declarándolo). Ninguna frase sin fuente.
%% IA: prohibida en la discusión crítica de la evidencia (punto 6.1).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Tendencias y riesgos}
\label{sec:riesgos}

Las plataformas sin servidor absorben cargas que antes exigían instancias siempre encendidas. El kernel distribuido que Huawei opera en producción sobre casi 20 regiones, sostiene microservicios, analítica y servicio de modelos, y migrarlos desde Spring a esa plataforma redujo el costo de recursos en un 90\,\%~\parencite{chen2024yuanrong}. La contraevidencia es doble. El sistema es propietario y reproduce el encierro de la sección~\ref{sec:limites_dependencias}, y el catálogo de Taibi, Kehoe y Poccia, sobre 91 entrevistas, ubica el problema dominante en la composición, la depuración y las pruebas, no en la falta de un entorno unificado~\parencite{taibi2022badpractices}. En el borde tropieza con el arranque en frío. Las funciones de inferencia cargan bibliotecas de aprendizaje automático, quedan limitadas por entrada y salida y pagan latencias de inicio largas, y esa es la penalización que cuantifica la sección~\ref{sec:poc}~\parencite{xie2021}.

El framework "Skyler" formaliza el riesgo económico. Modela el flujo de control y la tarifación de cada API para exponer \emph{economic sinks}, rutas donde una sola llamada mal acotada dentro de un bucle acumula miles de dólares con un costo de cómputo mínimo, y esa superficie habilita ataques de denegación de billetera contra endpoints públicos~\parencite{ribeiro2026skyler}. Prime Video lo exhibe en producción, su monitoreo de audio y video orquestaba funciones Lambda con Step Functions y pasaba los cuadros por S3 hasta chocar con el límite de transiciones de la cuenta, y consolidarlo en un proceso único sobre EC2 y ECS recortó el costo en un 90\,\%~\parencite{gancarz2023infoq}. El modelo de la sección~\ref{sec:costos} cotiza invocaciones y GB-segundo, así que ese gasto de orquestación y almacenamiento queda fuera de la curva. En arquitecturas muy descompuestas, el cruce real llega antes de los 24 millones de solicitudes mensuales. La contraevidencia acota el caso. El 82\,\% de las 89 aplicaciones usa cinco funciones o menos, con granularidad más cercana a un microservicio que a una función~\parencite{eismann2021}.

El borde suma un riesgo que la ficha no lista. Sus nodos carecen de la protección física de un centro de datos y corren sobre contenedores con aislamiento más débil que una máquina virtual, expuestos a ataques directos por vulnerabilidades del kernel~\parencite{xie2021}. La comparación de aislamientos de la sección~\ref{sec:wasm_wasi} se lee entonces como criterio de seguridad. El paso a OPEX puro del caso TERABYTE (sección~\ref{sec:caso}) conecta ambos riesgos y convierte cada decisión de descomposición en una factura recurrente, que crece con el tráfico que trae el propio éxito del sistema. La reserva práctica acota serverless a las cargas intermitentes y audita los \emph{economic sinks} en la etapa de diseño, antes del primer despliegue.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> CONCLUSIONES Y RECOMENDACIÓN                       Responsable: todo el grupo
%% Presupuesto: 1 página / ~450 palabras.
%% Responder literalmente la pregunta de la ficha: en qué condiciones de
%% carga conviene cada modelo (FaaS, contenedor serverless, edge, always-on)
%% en costo, latencia y complejidad operacional. Terminar con UNA
%% recomendación con la que el grupo se compromete (punto 4).
%% No resumir el informe.
%% IA: prohibida (punto 6.1). Todos deben poder defenderla oralmente.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\phantomsection
\addcontentsline{toc}{section}{Conclusiones y recomendación}
\section*{Conclusiones y recomendación}

%% [BORRADOR IA — sección del punto 6.1: el grupo la reescribe con sus
%% palabras y todos deben poder defenderla oralmente. Este texto solo ordena
%% los hechos ya establecidos en el informe; no agrega ninguno.]

Las funciones como servicio convienen a cargas cortas, intermitentes y sin estado local, activadas por eventos o solicitudes, cuyo volumen queda por debajo del punto de equilibrio: en el escenario base, 24,0 millones de solicitudes mensuales frente a dos instancias reservadas, y tan bajo como 5,3 millones con funciones de más memoria y duración (sección~\ref{sec:costos}). Pagan esa conveniencia con un arranque en frío de cientos de milisegundos (395~ms medianos en Lambda, sección~\ref{sec:poc}) y con topes de tiempo y carga útil que obligan a externalizar el estado (sección~\ref{sec:limites_dependencias}).

Los contenedores sin servidor extienden ese modelo a procesos más largos y a imágenes propias, con techos de una hora y decenas de gigabytes, pero no siempre escalan a cero y su arranque en frío fue el mayor medido (720~ms en Cloud Run). Son la opción para cargas variables que ya están empaquetadas como contenedor y toleran ese retardo o lo mitigan con instancias mínimas, acercándose entonces al costo de la capacidad reservada (sección~\ref{sec:coldstart}).

El cómputo sin servidor en el borde elimina el arranque en frío perceptible (1~ms en Workers) y acerca la respuesta al cliente, a cambio de aceptar solo JavaScript y WebAssembly, memoria de 128~MB por aislado y APIs propias que limitan la portabilidad (secciones~\ref{sec:modelos} y~\ref{sec:wasm_wasi}). Conviene a lógica ligera en la ruta del usuario: portales, notificaciones, validación y enrutamiento.

Las instancias siempre encendidas siguen siendo la opción para el núcleo de un sistema con tráfico sostenido, estado voluminoso, latencia acotada bajo el segundo y continuidad sin enlace, porque no imponen límites de plataforma y su costo es previsible; su precio es la operación completa del lado del equipo (sección~\ref{sec:cuadro}) y la capacidad ociosa cuando la carga cae.

Para la licitación de TERABYTE el grupo recomienda una arquitectura mixta: el núcleo operacional, el \emph{gate} y el patio sobre cómputo local siempre encendido y contenedores gestionados, y el borde sin servidor para el portal público y las notificaciones (sección~\ref{sec:caso}). El volumen del terminal no justifica por sí solo esa decisión, la justifican la cota de latencia de la cabina de grúa, la autonomía de 72~horas y el riesgo de una factura que crezca con cada movimiento (sección~\ref{sec:riesgos}). Antes del primer despliegue, cada camino de facturación por evento debe auditarse y acotarse en la etapa de diseño.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> BIBLIOGRAFÍA                                       Responsable: todos
%% Entradas en referencias.bib; citar con \parencite{clave}.
%% Mínimo 10 fuentes académicas (papers revisados por pares, libros,
%% estándares) + fuentes técnicas (docs oficiales). Cada uno aporta 1 o 2
%% académicas de su sección.
%% Preferentes: docs oficiales, NIST/ISO/IETF/CNCF/OWASP/ASF, papers,
%% bibliotecas PUCV. Admisibles declarándolo: informes de industria (muestra
%% y metodología), blogs de ingeniería. NO admisibles como primarias:
%% comparativas de un proveedor sobre su categoría, sin autor, agregadores.
%% Toda referencia se abre y verifica en el original. Referencia inexistente
%% = error grave. Entradas con precios: incluir región y fecha de consulta
%% (campo note o urldate).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\phantomsection
\addcontentsline{toc}{section}{Bibliografía}
\printbibliography[title={Bibliografía}]

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> ANEXO A. DECLARACIÓN DE USO DE INTELIGENCIA ARTIFICIAL
%%                                          Responsable: Daniel recopila;
%%                                          cada integrante declara lo suyo.
%% OBLIGATORIO aunque todos declaren nivel 0 (punto 6.3). Sin este anexo el
%% informe no se recibe conforme. Prevalece sobre la lámina de Pautas que
%% decía "Anexos (opcional)".
%% Numeración A1, A2, ... (macro \anexo, definida en el preámbulo; cada anexo
%% reinicia páginas, tablas y figuras con su propia letra).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\anexo{A}{Anexo A: Declaración de uso de inteligencia artificial}
\label{anexo:ia}

\textoPortadaFont{\textbf{Empresa:} N.\textordmasculine\ 6 --- TERABYTE\\}
\textoPortadaFont{\textbf{Tema:} TI-05 --- Serverless y computación en el borde (edge computing)\\}
\textoPortadaFont{\textbf{Fecha:} 21 de septiembre de 2026\\}

\bigskip

\subsection*{A. Declaración por sección e integrante}

%% Una fila por par sección–integrante (el punto 6.3 exige nivel POR SECCIÓN y
%% declaración INDIVIDUAL). Cada integrante agrega sus filas; nadie declara por
%% otro. En las secciones del punto 6.1 el nivel solo puede ser 0 o 1.
%% "Evidencia": ruta en anexo-ia/ del repositorio (ver parte B).

{\scriptsize
\begin{longtable}{|p{4.2cm}|p{2.4cm}|p{0.9cm}|p{3.0cm}|p{3.4cm}|}
    \hline
    \textbf{Sección del informe} & \textbf{Integrante} & \textbf{Nivel} & \textbf{Herramienta y versión} & \textbf{Evidencia (niveles 2 y 3)} \\
    \hline
    \endhead
    Resumen ejecutivo & Daniel Miranda & \textit{[TODO: 1 si se reescribe; 2 si se conserva el borrador]} & Claude (Cowork, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    Introducción y contexto (incl. párrafo de aporte) & Daniel Miranda & \textit{[TODO: párrafo de aporte 0 o 1 obligatorio (6.1)]} & Claude (Cowork, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    1. Marco teórico y conceptual & Matías Reyes & & & \\ \hline
    2.1 Modelos de ejecución y aislamiento & Matías Reyes & & & \\ \hline
    2.2 Arranque en frío & Isidora Cisternas & & & \\ \hline
    2.3 Facturación por consumo y punto de equilibrio & Francisca Abarca & & & \\ \hline
    2.4 Diseño orientado a eventos & Valentina Guzmán & & & \\ \hline
    2.5 WebAssembly y WASI & Isidora Cisternas & & & \\ \hline
    2.6 Límites técnicos y dependencia del proveedor & Fabián Solís & & & \\ \hline
    3.1 Alternativas adicionales & Daniel Miranda & 2 & Claude (Cowork, \texttt{claude-fable-5-1}); Grok (xAI) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    3.1 Alternativas adicionales (búsqueda y verificación de candidatos) & Claudio Toledo & 2 & Claude Code 2.1.271/2.1.275/2.1.278 (\texttt{claude-opus-5}); Claude (Cowork) & \texttt{anexo-ia/claudio-toledo/} \\ \hline
    3.2 Criterios (párrafo de criterios) & Valentina Guzmán & & & \\ \hline
    3.2 Cuadro comparativo y análisis & Rodolfo Fernández & & & \\ \hline
    3.2 Cuadro comparativo (filas Google, borde y código abierto; consolidación de las filas de AWS y Azure) & Claudio Toledo & 2 & Claude Code 2.1.271/2.1.275/2.1.278 (\texttt{claude-opus-5}); Claude (Cowork) & \texttt{anexo-ia/claudio-toledo/} \\ \hline
    3.2 Cuadro comparativo (volcado de las tablas a \LaTeX) & Claudio Toledo & 1 & Claude Code 2.1.271/2.1.275/2.1.278 (\texttt{claude-opus-5}); Claude (Cowork) & \texttt{anexo-ia/claudio-toledo/} \\ \hline
    3.2 Cuadro comparativo (dos párrafos de análisis) & Claudio Toledo & 0 & --- & --- \\ \hline
    3.2 Cuadro comparativo (estructura de tablas y planilla) & Daniel Miranda & 2 & Claude (Cowork, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    4.1 Prueba de concepto (incl. código auxiliar) y Anexo B & Vicente Arratia & 2 & Claude (Cowork, \texttt{claude-fable-5-1}); Claude Code 2.1.278 (\texttt{claude-opus-5}, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/vicente-arratia/} \\ \hline
    4.2 Modelo de costos y Anexo C & Francisca Abarca & & & \\ \hline
    4.3 Aplicación al caso TERABYTE & Fabián Solís & & & \\ \hline
    5. Tendencias y riesgos & Fabián Solís & & & \\ \hline
    Conclusiones y recomendación & todo el grupo & \textit{[TODO: 0 o 1 obligatorio (6.1); existe un borrador IA que se reescribe]} & Claude (Cowork, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    Anexo D: búsqueda documentada de alternativas & Daniel Miranda & 2 & Claude (Cowork, \texttt{claude-fable-5-1}); Grok (xAI) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    Plantilla LaTeX, bibliografía y organización del repositorio & Daniel Miranda & 2 & Claude (Cowork, \texttt{claude-fable-5-1}) & \texttt{anexo-ia/daniel-miranda/} \\ \hline
    Bibliografía (verificación de cada referencia) & cada autor de sección & & & \\ \hline
    Bibliografía (fuentes a su cargo: traducción de resúmenes y formato de planilla) & Claudio Toledo & 2 & Claude Code 2.1.271 (\texttt{claude-opus-5}) & \texttt{anexo-ia/claudio-toledo/} \\ \hline
    Cuestionario de 30 preguntas & cada autor de sección & & & \\ \hline
    Presentación (diapositivas) & todo el grupo & & & \\ \hline
\end{longtable}
}

\subsection*{B. Prompts utilizados (niveles 2 y 3)}

Los prompts efectivamente empleados se entregan íntegros y en orden cronológico en la carpeta \texttt{anexo-ia/} del repositorio del grupo, con una subcarpeta por integrante: \texttt{anexo-ia/<nombre-apellido>/B-prompts.md} contiene los prompts literales agrupados por sesión y herramienta, con el nivel declarado por sección; \texttt{C-evidencia.md} indexa las conversaciones y los commits del integrante; \texttt{registros/} guarda las exportaciones de cada conversación. El archivo \texttt{anexo-ia/README.md} describe el estándar aplicado y contiene el índice general, que se reproduce a continuación.

%% TODO (Daniel, al cierre): reemplazar esta tabla por el índice final de
%% anexo-ia/README.md, una fila por integrante.
{\scriptsize
\begin{longtable}{|p{0.6cm}|p{2.8cm}|p{4.3cm}|p{1.3cm}|p{4.9cm}|}
    \hline
    \textbf{\#} & \textbf{Integrante} & \textbf{Sección(es)} & \textbf{Nivel} & \textbf{Archivos en \texttt{anexo-ia/}} \\
    \hline
    \endhead
    1 & Vicente Arratia & 4.1 Prueba de concepto; Anexo B & 2 & \texttt{vicente-arratia/B-prompts.md}, \texttt{C-evidencia.md}, \texttt{registros/}, \texttt{cowork/} \\ \hline
    2 & Daniel Miranda & 3.1; Anexo D; plantilla y repositorio; Anexo A & 2 (búsqueda, verificación, formato); 1 (estilo en 3.1) & \texttt{daniel-miranda/B-prompts.md}, \texttt{C-evidencia.md}, \texttt{registros/} \\ \hline
    3 & Claudio Toledo & 3.1 (búsqueda y verificación); 3.2 (filas y consolidación); bibliografía de sus fuentes & 2 (búsqueda y verificación, traducción, esquemas); 1 (\LaTeX); 0 (análisis de 3.2) & \texttt{claudio-toledo/B-prompts.md}, \texttt{C-evidencia.md}, \texttt{registros/}, \texttt{registros/cowork/} \\ \hline
    4 & \textit{[TODO: cada integrante agrega su fila]} & & & \\ \hline
\end{longtable}
}

\subsection*{C. Evidencia trazable adjunta (niveles 2 y 3)}

La evidencia se entrega por tres medios, todos accesibles al profesor:

\begin{enumerate}[nosep]
    \item \textbf{Repositorio del grupo con su historial de commits:} \url{https://github.com/DmV554/informe-investigacion} (público). La versión entregada corresponde al commit \texttt{\textit{[TODO: hash del commit de entrega]}} de la rama \texttt{main}. El historial por rama muestra quién escribió cada sección y cuándo; los commits realizados con apoyo de IA llevan la coautoría declarada en el mensaje.
    \item \textbf{Conversaciones:} enlaces a las sesiones cuando la herramienta los provee y exportaciones completas en \texttt{anexo-ia/<integrante>/registros/}, indexadas en cada \texttt{C-evidencia.md}.
    \item \textbf{Historial del documento:} el propio historial de \texttt{informe/main.tex} y \texttt{informe/referencias.bib} en el repositorio (\texttt{git log -{}-follow}), que permite reconstruir cómo se escribió cada bloque.
\end{enumerate}

\subsection*{D. Firma de los integrantes}

%% Texto del formulario oficial: "Declaro que la información consignada en
%% este formulario es completa y veraz, que las secciones señaladas en el
%% punto 6.1 de las indicaciones son de autoría humana y que puedo explicar
%% oralmente cualquier parte del trabajo entregado."
%% Cada integrante completa su fila. Nadie firma por otro.

Declaro que la información consignada en este formulario es completa y veraz, que las secciones señaladas en el punto 6.1 de las indicaciones son de autoría humana y que puedo explicar oralmente cualquier parte del trabajo entregado.

{\scriptsize
\begin{longtable}{|p{0.8cm}|p{5.9cm}|p{3.6cm}|p{4.0cm}|}
    \hline
    \textbf{N.\textordmasculine} & \textbf{Nombre completo} & \textbf{Nivel máximo declarado} & \textbf{Firma} \\
    \hline
    \endhead
    1 & Francisca Antonia Abarca Pezo & & \\ \hline
    2 & Vicente André Arratia Caroca & & \\ \hline
    3 & Isidora Valentina Cisternas Cisternas & & \\ \hline
    4 & Rodolfo Antonio Fernández Vera & & \\ \hline
    5 & Valentina Ignacia Guzmán Elgueta & & \\ \hline
    6 & Daniel Ignacio Miranda Vázquez & & \\ \hline
    7 & Matias Ignacio Reyes Pizarro & & \\ \hline
    8 & Fabián Andres Solís Piñones & & \\ \hline
    9 & Claudio Patricio Toledo Mac-lean & 2 & \\ \hline
\end{longtable}
}

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> ANEXO B. METODOLOGÍA Y MEDICIONES DE LA PRUEBA DE CONCEPTO   Responsable: Vicente
%% Configuración desplegada, criterio de clasificación frío/caliente, notas
%% metodológicas y referencia al repositorio con los archivos exactos usados.
%% El código no se transcribe: vive en la carpeta poc/ (rama PoC).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\anexo{B}{Anexo B: Metodología y mediciones de la prueba de concepto}
\label{anexo:poc}

\subsection*{B.1 Configuración de las plataformas}

\tabla{Configuración desplegada}{poc-config}{|p{2.6cm}|p{3.4cm}|p{3.4cm}|p{5cm}|}{%
    \filaTabla{\textbf{Plataforma} & \textbf{Región y aislamiento} & \textbf{Recursos} & \textbf{Evento que induce el frío}}
    \filaTabla{AWS Lambda & us-east-1; microVM & 128~MB (CPU proporcional a la memoria \parencite{awsLambdaMemory}), Node.js 24.x, Function URL \parencite{awsLambdaUrls} & Cambio de la variable de entorno \texttt{POC\_MARKER}: el entorno de ejecución queda inválido y la invocación siguiente crea uno nuevo}
    \filaTabla{Cloudflare Workers & Borde (colo observado: GIG); V8 isolate \parencite{cloudflarehowworkersworks} & 128~MB fijos \parencite{cloudflare2026workerslimits}, plan Free & Publicación de una versión con \texttt{wrangler deploy}: las peticiones siguientes llegan a isolates nuevos (\texttt{first\_request})}
    \filaTabla{Google Cloud Run & us-east4; contenedor gen1 (gVisor \parencite{gcprunenv}) & 256~MiB, 1 vCPU, sin startup CPU boost \parencite{gcrCpu}, CPU solo durante solicitudes \parencite{gcrCpuAllocation}, min 0 / máx 1 instancia, concurrencia 1, Node.js 24 & \texttt{GET /salir}: el proceso responde y termina; con 0 instancias, la petición siguiente crea el contenedor}
}

\subsection*{B.2 Criterio de clasificación frío/caliente}

Cada petición se clasifica con lo que devuelve la función, nunca con su latencia:
en Lambda, frío si el \texttt{instance\_id} es distinto al de la petición anterior
(las peticiones secuenciales reutilizan el mismo entorno de ejecución; la fase de
inicialización de ese entorno es la que la plataforma reporta como \texttt{Init Duration}
\parencite{awsLambdaLifecycle}); en Workers, frío solo si
la instancia declara \texttt{first\_request}, porque Cloudflare reparte peticiones
consecutivas entre varios isolates vivos del mismo punto de presencia y el cambio
de identificador no implica arranque; en Cloud Run, frío si la edad del contenedor al
responder (\texttt{uptime\_ms}) es menor que la latencia medida de extremo a
extremo, es decir, si el contenedor nació dentro de la ventana de la petición. Este
último criterio no usa umbral: si el contenedor es más joven que la petición, solo
pudo nacer después de que ella salió del cliente.

\subsection*{B.3 Notas metodológicas sobre Cloud Run}

Con 128~MiB el contenedor excedía el límite de memoria (los registros del servicio
reportan entre 128 y 158~MiB usados) y era terminado entre peticiones, como documenta
la plataforma para ese caso \parencite{gcp2026runmemory}; eso invalidaba la medición
caliente, así que se subió a 256~MiB y la diferencia se declara (gen1 es la generación
que admite menos de 512~MiB \parencite{gcprunenv}).
Inducir el frío creando una revisión nueva \parencite{gcrResourceModel} no expone el
arranque al cliente. Cloud Run configura por defecto una sonda de arranque TCP y
considera el contenedor listo cuando la pasa \parencite{gcrHealthchecks,gcrContainerContract};
en la práctica observada, cuando el comando de despliegue retorna ya existe un contenedor
despierto, y en 24 de 25 ciclos la primera petición llegó a él (corrida complementaria
de las 00:05 de la misma noche). Esto último es observación propia, no afirmación de
la documentación. Por eso se induce con la terminación del proceso
(\texttt{GET /salir}, activo solo con la variable \texttt{POC\_SALIR\_HABILITADO=1}).
Tras la terminación se esperan 2~s: sin esa espera la petición llega a la instancia
que está muriendo (HTTP 503). La latencia de arranque de contenedor que registra la
propia plataforma (métrica \texttt{container/startup\_latencies}, ``Container startup
latency'', distribución en ms \parencite{gcpMetricsRun,gcrMonitoring}; ventana de la
corrida) fue de 26 arranques con media 1\,014~ms y p95 aproximado 1\,119~ms;
es otro instrumento (arranque visto por la plataforma, no latencia extremo a
extremo) y se reporta solo como contraste. Nota de despliegue: en proyectos nuevos la
cuenta de servicio por defecto de Compute ya no recibe el rol Editor
\parencite{gcpOrgPolicySA}, por lo que el primer despliegue desde código fuente falla
con HTTP 403 hasta otorgarle \texttt{roles/cloudbuild.builds.builder}.

\subsection*{B.4 Repositorio y archivos utilizados}

El código, la configuración y los datos están en el repositorio del grupo, carpeta
\path{poc/}, rama \texttt{PoC}. El código auxiliar (funciones y scripts) se generó
con asistencia de IA y fue revisado y ejecutado por el autor; se declara en el
Anexo~A. Los archivos de los que salen la Tabla~\ref{tab:poc-resumen} y la
Figura~\ref{fig:poc} son exactamente los siguientes:

\begin{itemize}
    \item \path{functions/lambda/index.mjs}, \path{functions/cloudflare/worker.js} y
          \path{functions/cloudrun/index.js}: la función desplegada en cada plataforma.
    \item \path{config.json}: URL de cada despliegue y parámetros de la corrida
          (ciclos, peticiones calientes por ciclo, esperas).
    \item \path{scripts/medir.py}: invoca cada URL por HTTP, mide la latencia total,
          clasifica con el criterio de B.2 y escribe una fila por petición.
    \item \path{scripts/analizar.py}: produce \path{results/resumen.csv}, las filas
          de la Tabla~\ref{tab:poc-resumen} y la Figura~\ref{fig:poc}.
    \item \path{scripts/startup_cloudrun.py}: consulta la métrica de arranque de
          contenedor de Cloud Monitoring para la ventana de la corrida.
    \item \path{data/mediciones_20260921-0041_forzado.csv}: corrida fuente
          (450 filas, una por petición). La corrida complementaria de las 00:05 citada
          en B.3 es \path{data/mediciones_20260921-0005_forzado.csv}.
    \item \path{results/resumen.csv}, \path{results/cloudrun_startup.csv} y
          \path{results/poc-coldstart.pdf}: resumen estadístico, latencia de arranque
          registrada por Google Cloud y figura.
\end{itemize}

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> ANEXO C. PLANILLA DEL MODELO DE COSTOS               Responsable: Francisca
%% Tabla completa por escenario de carga (por ejemplo 10 mil, 100 mil, 1
%% millón y 10 millones de solicitudes/mes), fórmulas, y la cita de cada
%% precio con producto, moneda, región, fecha y tipo. Puede ir como tabla
%% LaTeX o como PDF externo (\pdfExterno{assets/pdf/...}{...}{...}).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\anexo{C}{Anexo C: Planilla del modelo de costos}
\label{anexo:costos}\label{anx:planilla}

\subsection*{Parámetros y fórmulas}

Precios de lista de las Tablas~3.2a y~3.2b (us-east-1, USD, consultados el 20-09-2026). Un mes equivale a 730 horas (2.628.000 s). Con $N$ solicitudes al mes:
\[
C_L(N)=\max(0,N-10^{6})\,c_r+\max(0,\,N\,m\,d-4\cdot10^{5})\,c_g,\qquad
C_F(N)=k(N)\cdot p\cdot730,
\]
\[
q_{\mathrm{pico}}=f\cdot\frac{N}{2{,}628\cdot10^{6}},\qquad
k(N)=\max\!\left(2,\left\lceil \frac{q_{\mathrm{pico}}}{q}\right\rceil\right).
\]
El volumen de equilibrio es el menor $N$ tal que $C_L(N)\ge C_F(N)$. Mientras $k=2$ y $N>4\cdot10^{6}$ (sobre la capa gratuita), la solución cerrada es
$N^{*}=\frac{2\,p\cdot730+10^{6}\,c_r+4\cdot10^{5}\,c_g}{c_r+m\,d\,c_g}\approx24{,}0\cdot10^{6}$.

{\small
\begin{center}
\begin{tabular}{|l|p{6.0cm}|p{2.6cm}|}
\hline
\textbf{Símbolo} & \textbf{Descripción y valor} & \textbf{Origen} \\ \hline
$c_r$ & Precio por solicitud de Lambda: 0,20 USD por millón & Tabla 3.2a \\ \hline
$c_g$ & Cómputo de Lambda x86: 0,0000166667 USD/GB-s & Tabla 3.2a \\ \hline
--- & Capa gratuita mensual: 1 M de solicitudes y 400.000 GB-s & Tabla 3.2a \\ \hline
$p$ & \texttt{t3.medium} reservada 1 año: 0,026 USD/h (18,98 USD/mes); 3 años: 0,017 USD/h; bajo demanda: 0,0416 USD/h & Tabla 3.2a \\ \hline
$m,\ d$ & Memoria 0,5 GB y duración 0,2 s por solicitud & Supuesto \\ \hline
$f$ & Pico igual a 4 veces el promedio & Supuesto \\ \hline
$q$ & 20 solicitudes/s sostenidas por instancia (20 ms de CPU por solicitud) & Supuesto \\ \hline
\end{tabular}
\end{center}
}
\clearpage

\subsection*{Costo mensual por volumen (escenario base)}

{\small\setlength{\tabcolsep}{3.5pt}
\begin{center}
\begin{tabular}{|r|r|r|r|r|r|l|}
\hline
\textbf{$N$ (M/mes)} & \textbf{Prom. (req/s)} & \textbf{Pico (req/s)} & \textbf{$k$} & \textbf{Lambda (USD)} & \textbf{Flota (USD)} & \textbf{Menor costo} \\ \hline
2 & 0,76 & 3,04 & 2 & 0,20 & 37,96 & Lambda \\ \hline
5 & 1,90 & 7,61 & 2 & 2,47 & 37,96 & Lambda \\ \hline
10 & 3,81 & 15,22 & 2 & 11,80 & 37,96 & Lambda \\ \hline
15 & 5,71 & 22,83 & 2 & 21,13 & 37,96 & Lambda \\ \hline
20 & 7,61 & 30,44 & 2 & 30,47 & 37,96 & Lambda \\ \hline
24 & 9,13 & 36,53 & 2 & 37,93 & 37,96 & Lambda \\ \hline
26 & 9,89 & 39,57 & 2 & 41,67 & 37,96 & Flota fija \\ \hline
28 & 10,65 & 42,62 & 3 & 45,40 & 56,94 & Lambda \\ \hline
30 & 11,42 & 45,66 & 3 & 49,13 & 56,94 & Lambda \\ \hline
35 & 13,32 & 53,27 & 3 & 58,47 & 56,94 & Flota fija \\ \hline
40 & 15,22 & 60,88 & 4 & 67,80 & 75,92 & Lambda \\ \hline
45 & 17,12 & 68,49 & 4 & 77,13 & 75,92 & Flota fija \\ \hline
50 & 19,03 & 76,10 & 4 & 86,47 & 75,92 & Flota fija \\ \hline
55 & 20,93 & 83,71 & 5 & 95,80 & 94,90 & Flota fija \\ \hline
60 & 22,83 & 91,32 & 5 & 105,13 & 94,90 & Flota fija \\ \hline
\end{tabular}
\end{center}
}

Lambda es más barata hasta 24,0~M. Luego la ventaja cambia de signo con cada instancia adicional: Lambda vuelve a ser más barata entre 26,3 y 34,2~M, entre 39,4 y 44,4~M y entre 52,6 y 54,5~M, y desde 54,5~M la flota fija es siempre más barata. El costo marginal de la flota a plena carga (18,98 USD por 13,14~M de solicitudes) es 1,44 USD por millón, menor que el de Lambda (1,87 USD por millón).

\subsection*{Sensibilidad: memoria y duración}

Primer cruce, en millones de solicitudes al mes; ``no cruza'' indica que Lambda cuesta menos por solicitud que la flota a plena carga.

{\small
\begin{center}
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Memoria (MB) $\backslash$ duración} & \textbf{100 ms} & \textbf{200 ms} & \textbf{500 ms} \\ \hline
256 & no cruza & no cruza & 19,6 \\ \hline
512 & no cruza & 24,0 & 10,3 \\ \hline
1024 & 24,0 & 12,7 & 5,3 \\ \hline
\end{tabular}
\end{center}
}

\subsection*{Variantes del comparador y de los supuestos}

{\small
\begin{center}
\begin{tabular}{|p{4.6cm}|p{5.4cm}|c|}
\hline
\textbf{Variante} & \textbf{Cambio} & \textbf{Cruce (M/mes)} \\ \hline
Base: 2 instancias, reserva 1 año & 0,026 USD/h por instancia & 24,0 \\ \hline
Reserva 3 años & 0,017 USD/h por instancia & 17,0 \\ \hline
Bajo demanda & 0,0416 USD/h por instancia & no cruza \\ \hline
Sin capa gratuita de Lambda & Base, sin descuentos de Lambda & 20,3 \\ \hline
Pico = 2 veces el promedio & Capacidad mensual por instancia se duplica & 24,0 \\ \hline
Pico = 8 veces el promedio & Capacidad mensual por instancia se reduce a la mitad & no cruza \\ \hline
Capacidad de 10 solicitudes/s por instancia & 40 ms de CPU por solicitud & no cruza \\ \hline
Base + NAT (solo cargo por hora) & 0,045 USD/h = 32,85 USD/mes & 102,6 \\ \hline
2 tareas Fargate 0,5 vCPU y 1 GB & Capacidad 25 solicitudes/s por tarea & 23,0 \\ \hline
\end{tabular}
\end{center}
}
\newpage

\subsection*{Código auxiliar (Python)}

El siguiente script reproduce el escenario base, la sensibilidad y las variantes de comparador (el cálculo de las tablas de esta planilla usa las mismas funciones).

{\scriptsize
\begin{verbatim}
# Modelo de costos 4.2, precios Tablas 3.2a y 3.2b (us-east-1, USD, 20-09-2026)
# Salida: filas LaTeX con coma decimal, listas para pegar en el Anexo C.

import math

H = 730; SEC = H * 3600                          # horas y segundos por mes
C_REQ, C_GBS = 0.20 / 1e6, 0.0000166667          # Lambda x86: USD/solic. y USD/GB-s
FREE_REQ, FREE_GBS = 1_000_000, 400_000          # capa gratuita mensual de Lambda
P_H = {"1a": 0.026, "3a": 0.017, "od": 0.0416}   # EC2 t3.medium, USD por hora
P_FARGATE = 0.5 * 0.04048 + 1.0 * 0.004445       # tarea Fargate 0,5 vCPU + 1 GB, USD/h
NAT_H = 0.045                                    # NAT Gateway, USD por hora

def lambda_cost(N, m=0.5, d=0.2, free=True):
    """Costo mensual de Lambda: N solicitudes, m GB, d segundos."""
    fr, fg = (FREE_REQ, FREE_GBS) if free else (0, 0)
    return max(0, N - fr) * C_REQ + max(0, N * m * d - fg) * C_GBS

def fleet_cost(N, p=P_H["1a"], cap=20, f=4, kmin=2, extra=0.0):
    """Flota: k unidades a p USD/h, cap req/s c/u; pico = f x promedio."""
    k = max(kmin, math.ceil(f * (N / SEC) / cap - 1e-12))
    return k * p * H + extra

def first_cross(fleet, m=0.5, d=0.2, free=True, hi=400e6, step=1e4):
    """Menor N con costo Lambda >= costo flota; None si no ocurre hasta hi."""
    N = step
    while N <= hi:
        if lambda_cost(N, m, d, free) >= fleet(N):
            return N
        N += step
    return None

def c(x, nd=2):                                  # numero con coma decimal
    return "no cruza" if x is None else f"{x:.{nd}f}".replace(".", ",")

def fila(*cols):                                 # fila de tabla LaTeX
    print(" & ".join(str(x) for x in cols) + r" \\ \hline")

def M(x):                                        # N en millones, o None
    return None if x is None else x / 1e6

base = lambda N: fleet_cost(N)

print("% Equilibrio base (M/mes):", c(M(first_cross(base)), 1))
print("% Solucion cerrada (M/mes):", c((2*P_H["1a"]*H + FREE_REQ*C_REQ
      + FREE_GBS*C_GBS) / (C_REQ + 0.5*0.2*C_GBS) / 1e6, 1))

print("\n% Costo mensual por volumen (N, prom, pico, k, Lambda, flota, menor)")
for n in (0.01, 0.1, 1, 2, 5, 10, 15, 20, 24, 26, 28, 30, 35, 40, 45, 50, 55, 60):
    N = n * 1e6; avg = N / SEC
    k = max(2, math.ceil(4 * avg / 20 - 1e-12))
    l, fl = lambda_cost(N), fleet_cost(N)
    fila(c(n, 2) if n < 1 else c(n, 0), c(avg), c(4 * avg), k, c(l), c(fl),
         "Lambda" if l < fl else "Flota fija")

print("\n% Intervalos donde Lambda es mas barata (M/mes)")
ivs, ini, N = [], None, 1e4
while N <= 80e6:
    barata = lambda_cost(N) < fleet_cost(N)
    if barata and ini is None: ini = N
    if not barata and ini is not None: ivs.append((ini, N)); ini = None
    N += 1e4
print("%", [(c(M(a), 1), c(M(b), 1)) for a, b in ivs])

print("\n% Sensibilidad: filas 256/512/1024 MB; columnas 100/200/500 ms")
for mb in (256, 512, 1024):
    fila(mb, *[c(M(first_cross(base, mb / 1024, d)), 1) for d in (0.1, 0.2, 0.5)])

print("\n% Variantes del comparador y de los supuestos (primer cruce, M/mes)")
V = {
 "Base: 2 instancias, reserva 1 anio": (base, {}),
 "Reserva 3 anios": (lambda N: fleet_cost(N, p=P_H["3a"]), {}),
 "Bajo demanda": (lambda N: fleet_cost(N, p=P_H["od"]), {}),
 "Sin capa gratuita de Lambda": (base, {"free": False}),
 "Pico = 2 veces el promedio": (lambda N: fleet_cost(N, f=2), {}),
 "Pico = 8 veces el promedio": (lambda N: fleet_cost(N, f=8), {}),
 "Capacidad de 10 req/s por instancia": (lambda N: fleet_cost(N, cap=10), {}),
 "Base + NAT (solo cargo por hora)": (lambda N: fleet_cost(N, extra=NAT_H*H), {}),
 "2 tareas Fargate 0,5 vCPU y 1 GB":
     (lambda N: fleet_cost(N, p=P_FARGATE, cap=25), {}),
}
for nombre, (fl, kw) in V.items():
    fila(nombre, c(M(first_cross(fl, **kw)), 1))

print("\n% Barrido: primer cruce segun pico f y capacidad (razon r = f / cap)")
for f, cap in ((2, 20), (4, 20), (4.4, 20), (5, 20), (6, 20), (8, 20),
               (4, 25), (4, 18), (4, 16), (4, 15), (4, 10)):
    fila(f"f={c(f, 1)}", f"cap={cap}", c(f / cap, 3),
         c(M(first_cross(lambda N: fleet_cost(N, f=f, cap=cap))), 1))

# Umbral de "no cruza": la flota a plena carga cuesta p*H USD por (cap*SEC/f) solic.
lam = (C_REQ + 0.5 * 0.2 * C_GBS) * 1e6          # USD por millon de solicitudes
print("\n% Lambda cuesta", c(lam, 3), "USD/M; la flota a plena carga cuesta",
      "0,361*f*(20/cap) USD/M")
print("% no cruza si f/cap >=", c(lam * SEC / 1e6 / (P_H["1a"] * H), 4))
\end{verbatim}
}


\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> ANEXO D. BÚSQUEDA DOCUMENTADA DE ALTERNATIVAS   Responsables: Claudio y Daniel
%% Punto 4, "Declaración cuando no hay más": por cada una de las cinco
%% listas, fuentes consultadas, criterios de inclusión y descarte, fecha, y
%% candidatos descartados con su motivo (por ejemplo, producto descontinuado).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\anexo{D}{Anexo D: Búsqueda documentada de alternativas}

%% Estructura del anexo (punto 4 de las Indicaciones, "Declaración cuando no
%% hay más"): D.1 método común a las cinco listas; D.2 a D.6 un bloque por
%% lista con el registro de candidatos evaluados; D.7 lo que no se encontró.
%% La sección 3.1 del cuerpo dice el RESULTADO (qué entra y qué aporta) y
%% remite aquí con \ref{anexo:busqueda}; este anexo es la EVIDENCIA del
%% proceso. Registro de trabajo para llenar a mano mientras se busca:
%% trabajo/3.1-alternativas/registro-busqueda.md (misma estructura).
%% Todo lo que aparece aquí es de autoría del grupo (punto 6.1).
\label{anexo:busqueda}

\subsection*{D.1 Método de búsqueda}

% Vocabulario: "ficha" es la ficha del tema TI-05 en las Indicaciones del
% curso \parencite{moya2026indicaciones}; "lista" es cada una de sus cinco
% categorías de tecnologías a comparar. La definición general va en la
% introducción (TODO Daniel); aquí se repite en una frase para que el anexo
% sea autocontenido.

En este anexo, \emph{ficha} designa la ficha del tema TI-05 del documento de indicaciones del curso \parencite{moya2026indicaciones}, y \emph{lista} cada una de las cinco categorías de tecnologías que esa ficha pide comparar (funciones como servicio, contenedores sin servidor, cómputo en el borde, código abierto y datos sin servidor). \emph{Plataformas de la ficha} son las que la ficha nombra explícitamente en cada lista.

Los candidatos se identificaron en tres fuentes de descubrimiento: las tablas de plataformas de los surveys citados en el informe, la categoría \emph{serverless} del CNCF Landscape \parencite{cncf2026landscapeserverless} y la documentación oficial de los proveedores. La búsqueda se realizó entre el 19 y el 20 de septiembre de 2026, fecha en que se verificaron todas las fuentes citadas en las tablas D.2 a D.6.


Los criterios de la Tabla~\ref{tab:criterios-busqueda} se fijaron antes de evaluar el primer candidato y se aplicaron a todos por igual, en este orden: primero las puertas de descarte X1 y X2 a toda la pool de candidatos; luego los criterios de inclusión I1, I2 e I3 a los que quedaron; la regla de desempate se usó solo en las listas donde quedaron más de dos candidatos. Cuando quedaron exactamente dos, no se aplicó. Cuando quedó uno o ninguno, se declara en D.7.

\tabla{Criterios de inclusión y descarte aplicados a todos los candidatos}{criterios-busqueda}{|p{1.2cm}|p{6.6cm}|p{6.0cm}|}{%
    \filaTabla{\textbf{Código} & \textbf{Criterio} & \textbf{Cómo se verifica}}
    \filaTabla{I1 & Vigencia y verificabilidad: el producto se puede contratar hoy; sus límites técnicos están publicados en documentación oficial; y su precio de lista vigente está publicado, o el proveedor declara que solo se entrega por cotización. & Se abren la página oficial de límites y la de precios el día de la consulta. Un ejemplo o escenario con advertencia de que no refleja precios actuales no cuenta como precio publicado.}
    \filaTabla{I2 & Pertenencia a la lista: el producto encaja en el modelo de la lista evaluada como propósito principal, no como complemento de otro producto. En funciones, contenedores, borde y datos: ejecuta código del usuario, o consulta y almacena datos del usuario, bajo el modelo de esa lista. En código abierto: componente de código abierto para construir o ejecutar cargas serverless, sea plataforma, runtime o autoescalado, en línea con lo que la ficha ya incluye. & Se lee la descripción oficial del producto y se confirma que su función principal corresponde al modelo de la lista.}
    \filaTabla{I3 & Aporte diferencial: agrega al menos una dimensión no cubierta por las plataformas de la ficha en esa lista, entre estas seis: (1)~unidad o modelo de cobro; (2)~mecanismo de aislamiento o de escala a cero; (3)~otra jurisdicción o región de operación documentada (moneda de facturación, ubicación de los datos o de los puntos de presencia); (4)~acoplamiento a otro servicio del mismo ecosistema; (5)~límites en otro orden de magnitud (tiempo, payload, concurrencia); (6)~portabilidad entre despliegue gestionado y autoalojado. & Se identifica la dimensión en documentación oficial y se anota su número en el registro. No cuentan popularidad, madurez percibida, herramientas de desarrollo ni el mero hecho de ser un proveedor distinto.}
    \filaTabla{X1 & Solapamiento dentro de la misma lista: el candidato es una extensión, modo o variante de otro candidato o de una plataforma de la ficha en esa misma lista. Se aplica por lista, no entre listas. & Si se activa entre dos candidatos de la misma lista, se retiene el que gane la regla de desempate y se descarta el otro con este código.}
    \filaTabla{X2 & Retirado o archivado: el producto o proyecto ya no se puede contratar o ya no se mantiene. & Sitio o repositorio oficial: aviso de fin de soporte, de retiro o de archivo, con fecha.}
}

\FloatBarrier

\paragraph*{Regla de desempate.} Se activa solo cuando, tras aplicar X1, X2, I1, I2 e I3, quedan más de dos candidatos en una lista. Cuando dos de ellos aportan el mismo tipo de contraste frente a las plataformas de la ficha, se retiene el que resulte favorecido por el primer paso que los separe, en este orden: (1)~precio de lista vigente publicado frente a solo por cotización o precio ausente; (2)~límites técnicos publicados frente a no publicados; (3)~disponibilidad general frente a beta, beta abierta o vista previa, pública o privada; (4)~documentación oficial más específica para la lista evaluada (página de límites o de precios del propio servicio, no solo la descripción general). Si tras esos pasos siguen quedando más de dos candidatos con contrastes distintos entre sí, (5)~se retienen los dos cuya dimensión de aporte sea más útil para el resto del informe, indicando la sección que la usa (unidad de cobro para 2.3 y 4.2; escala a cero y arranque en frío para 2.2; portabilidad para 2.6; jurisdicción y región para 4.3). Un candidato descartado por esta regla se registra como ``desempate, paso~$n$'', no con un código de puerta.

% Nota de registro para las tablas D.2 a D.6 (no va en D.1): cada dato de
% precio se etiqueta como "precio de lista", "solo por cotización" o "no
% publicado"; cada dato de límite como "publicado" o "no publicado"; ambos
% con URL oficial y fecha de consulta.

%% ---------------------------------------------------------------------------
%% Un bloque por lista. Columnas del registro (5):
%%   Candidato · Fuente oficial (\parencite a la entrada del .bib, que lleva URL
%%   y urldate) · Fecha · Resultado (criterio): "Entra (I1--I3)", "Descartado
%%   (X2)", "Descartado (I1: límites no publicados)", "Descartado (desempate,
%%   paso n)" · Motivo / qué aporta.
%% "Motivo" es una frase: para los que entran, qué aportan a la comparación
%% (la misma frase que va a la Tabla 3.1); para los descartados, por qué.
%% Filas precargadas: pool completa revisada (guía del equipo + ampliación
%% verificada el 2026-09-20). Las filas con Resultado ya escrito son descartes
%% cerrados por hechos documentados (retiro, archivo, sucesión, no ejecuta
%% código); el resto lo decide Daniel aplicando D.1. Agregar/quitar libremente.
%% ---------------------------------------------------------------------------

\subsection*{D.2 Funciones como servicio}

Ya nombra la ficha: AWS Lambda, Google Cloud Run functions, Azure Functions. Cada dato de precio se etiquetó como precio de lista, solo por cotización o no publicado; cada límite como publicado o no publicado; la URL y la fecha de consulta de cada fuente están en la entrada bibliográfica citada.

{\scriptsize
\begin{longtable}{|p{2.5cm}|p{2.4cm}|p{1.4cm}|p{2.3cm}|p{5.2cm}|}
    \hline
    \textbf{Candidato} & \textbf{Fuente oficial} & \textbf{Fecha} & \textbf{Resultado (criterio)} & \textbf{Motivo / qué aporta} \\
    \hline
    \endhead
    Oracle Cloud Infrastructure Functions & \parencite{oracle2026ocifunctions,oracle2026ocifunctionslimits,oracle2026fnoverview} & 2026-09-20 & Entra (I1, I2, I3) & I3-6: la documentación oficial lo describe como construido sobre el motor de código abierto Fn Project, lo que permite un despliegue gestionado y uno autoalojado del mismo motor. Apoyo I3-5: invocación desacoplada con tiempo máximo de 3.600~s frente a 300~s en modo sincrónico. Límites y precio de lista en USD publicados; concurrencia aprovisionada cobrada al 25~\% de la tarifa mientras no se usa. \\ \hline
    Alibaba Cloud Function Compute 3.0 & \parencite{alibaba2026fcpayg,alibaba2026fclimits,alibaba2026fctrial} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-1: cobro por unidad de cómputo agregada, CU, que convierte vCPU, memoria e invocaciones a una sola unidad; I3-5: tiempo máximo 86.400~s, memoria hasta 32~GB). Con más de dos candidatos vivos de contraste distinto, el paso 5 retuvo a Oracle (portabilidad, útil en 2.6) y Scaleway (jurisdicción, útil en 4.3). \\ \hline
    IBM Cloud Code Engine (functions) & \parencite{ibm2026codeenginelimits,ibm2026codeenginesample} & 2026-09-20 & Descartado (I1) & Límites de functions publicados (tiempo de ejecución 120~s, código en línea 100~KB, memoria hasta 48.000~MB). El precio unitario solo aparece en un escenario oficial de estimación con advertencia expresa de que no refleja precios actuales; no cuenta como precio de lista vigente. \\ \hline
    Scaleway Serverless Functions & \parencite{scaleway2026serverlesspricing,scaleway2026functionslimits} & 2026-09-20 & Entra (I1, I2, I3) & I3-3: proveedor con operación documentada en la Unión Europea y precio de lista en euros (recurso por GB-s, solicitudes, recursos aprovisionados), con capa gratuita mensual. Límites publicados: tiempo 10~s a 60~min, concurrencia 1 por instancia, hasta 50 instancias, payload 6~MiB. Precio en USD no publicado; se declara. \\ \hline
    Huawei Cloud FunctionGraph & \parencite{huawei2026functiongraphprice,huawei2026functiongraphlimits} & 2026-09-20 & Descartado (desempate, paso 4) & Cumple I1--I3 (I3-5: duración máxima 259.200~s, memoria 10~GB). Comparte contraste con Alibaba (funciones internacionales con techos altos y cobro por solicitud y GB-s); en el paso 4 se retuvo Alibaba por documentación de cobro más específica (unidad CU definida en su propia página). \\ \hline
    Tencent Cloud SCF & \parencite{tencent2026scfpricing,tencent2026scffreetier,tencent2026scfquotas} & 2026-09-20 & Descartado (desempate, paso 4) & Cumple I1--I3 (I3-1: capa gratuita temporal de tres meses seguida de un paquete básico diario). Comparte contraste con Alibaba y Huawei; en el paso 4 se retuvo Alibaba. \\ \hline
\end{longtable}
}

\subsection*{D.3 Contenedores sin servidor}

Ya nombra la ficha: Google Cloud Run, AWS Fargate, Azure Container Apps, AWS App Runner. Cada dato de precio se etiquetó como precio de lista, solo por cotización o no publicado; cada límite como publicado o no publicado; la URL y la fecha de consulta de cada fuente están en la entrada bibliográfica citada.

{\scriptsize
\begin{longtable}{|p{2.5cm}|p{2.4cm}|p{1.4cm}|p{2.3cm}|p{5.2cm}|}
    \hline
    \textbf{Candidato} & \textbf{Fuente oficial} & \textbf{Fecha} & \textbf{Resultado (criterio)} & \textbf{Motivo / qué aporta} \\
    \hline
    \endhead
    Fly.io (Machines) & \parencite{fly2026pricing} & 2026-09-20 & Descartado (I1) & Precio de lista publicado (facturación por segundo de máquina encendida, cargo por disco raíz de máquinas detenidas, egreso por región). Los límites técnicos duros (tamaño máximo, número de máquinas, tiempo de ejecución) no están publicados como cifras fijas; sin límites publicados no cumple I1. \\ \hline
    Railway & \parencite{railway2026pricing} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-1: cuota mensual de plan que se abona al consumo, más RAM y CPU medidos por minuto y egreso por GB). Con tres candidatos vivos de contraste distinto, el paso 5 retuvo a Scaleway (jurisdicción, 4.3) y Render (escala a cero dependiente del plan, 2.2 y 4.2). \\ \hline
    Render & \parencite{render2026pricing,render2026limits,render2026faq} & 2026-09-20 & Entra (I1, I2, I3) & I3-2: dentro de la misma plataforma, el plan gratuito escala a cero (el servicio se apaga tras 15~min sin tráfico entrante) y los planes pagados no escalan a cero (el cómputo queda aprovisionado y se paga aunque no llegue tráfico). Ese comportamiento dependiente del plan no aparece en las plataformas de la ficha y es el fenómeno que modela la sección 4.2. Precio de lista publicado (instancia pagada más pequeña USD~7/mes) y límites por plan publicados. \\ \hline
    Scaleway Serverless Containers & \parencite{scaleway2026serverlesspricing,scaleway2026containerslimits} & 2026-09-20 & Entra (I1, I2, I3) & I3-3: operación documentada en la Unión Europea y precio de lista en euros por GB-s y por vCPU-s, con capa gratuita mensual de ambos. Límites publicados: tiempo 10~s a 60~min, concurrencia 80, hasta 50 instancias, escala a cero tras 15~min. Mismo proveedor que en D.2: X1 no aplica entre listas, lo que se declara en 3.1. \\ \hline
    Koyeb & \parencite{koyeb2026pricing,koyeb2026instances} & 2026-09-20 & Descartado (I3) & Cumple I1 e I2. Su escala a cero en disponibilidad general y su facturación por segundo ya están cubiertas en la lista por Cloud Run y Container Apps (escala a cero) y por Fargate y Cloud Run (facturación por segundo); la línea Eco es una tarifa reducida, no otra unidad de cobro. No aporta ninguna de las seis dimensiones. \\ \hline
    DigitalOcean App Platform & \parencite{do2026appplatformpricing,do2026appplatformlimits,do2026appplatformstz} & 2026-09-20 & Descartado (I3) & Cumple I1 e I2 (precio de entrada publicado, facturación por segundo, límites de aplicación publicados). Su única dimensión potencial, la escala a cero, está en vista previa privada y no se puede contratar en general; el resto (facturación por segundo, plan fijo) lo cubren Fargate y App Runner. \\ \hline
\end{longtable}
}

\subsection*{D.4 Cómputo en el borde}

Ya nombra la ficha: Cloudflare Workers (Durable Objects, D1, R2), Fastly Compute, AWS Lambda@Edge y CloudFront Functions, Vercel, Netlify, Deno Deploy. Cada dato de precio se etiquetó como precio de lista, solo por cotización o no publicado; cada límite como publicado o no publicado; la URL y la fecha de consulta de cada fuente están en la entrada bibliográfica citada.

{\scriptsize
\begin{longtable}{|p{2.5cm}|p{2.4cm}|p{1.4cm}|p{2.3cm}|p{5.2cm}|}
    \hline
    \textbf{Candidato} & \textbf{Fuente oficial} & \textbf{Fecha} & \textbf{Resultado (criterio)} & \textbf{Motivo / qué aporta} \\
    \hline
    \endhead
    Akamai EdgeWorkers & \parencite{akamai2026edgeworkerslimits,akamai2026edgeworkersbilling} & 2026-09-20 & Descartado (I1) & Límites por nivel publicados (memoria 1,5 / 2,5 / 4~MB; CPU 10 / 20 / 70~ms; tiempo total 4 / 5,5 / 10~s). La documentación no publica precio de lista ni declara expresamente la venta solo por cotización (remite a Marketplace y prueba gratuita); la página comercial no fue accesible. No cumple el segundo requisito de I1. \\ \hline
    Supabase Edge Functions & \parencite{supabase2026edgefunctionslimits,supabase2026edgefunctionsbilling} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-4: función de borde acoplada a la base Postgres del mismo proyecto con seguridad por filas; memoria 256~MB, CPU 2~s por solicitud, USD~2 por millón de invocaciones sobre la cuota). Con más de dos candidatos vivos de contraste distinto, el paso 5 retuvo a Azion (región, 4.3) y Bunny (modelo de cobro y límites, 2.3 y 4.2). \\ \hline
    Bunny Edge Scripting & \parencite{bunny2026scriptingpricing,bunny2026scriptinglimits} & 2026-09-20 & Entra (I1, I2, I3) & I3-1: el cómputo (USD~0,20 por millón de solicitudes más USD~0,02 por 1.000~s de CPU) se factura separado del ancho de banda de la CDN, que se cobra aparte. I3-5: hasta 30~s de CPU por solicitud, frente a límites de milisegundos en otras plataformas de borde de la ficha. Memoria 128~MB, 50 subsolicitudes, arranque documentado de 500~ms; tiempo total de reloj no publicado, se declara. \\ \hline
    Azion Edge Functions & \parencite{azion2026pricing,azion2026functionslimits} & 2026-09-20 & Entra (I1, I2, I3) & I3-3: proveedor con puntos de presencia y operación documentada en Latinoamérica, con precio de lista en USD (primeras 8~h de cómputo y 3~millones de invocaciones al mes sin cargo; luego USD~0,072 por 1.000~s y USD~0,30 por millón). Límites publicados: CPU 2~s y tiempo de reloj 5~min por solicitud, memoria 512~MB, 50 subsolicitudes, arranque máximo 2~s. \\ \hline
    Fermyon Cloud (Spin gestionado) & \parencite{fermyon2026cloudpricing,fermyon2026cloudfaq} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-6: servicio gestionado del mismo motor Spin que la ficha incluye como código abierto). Plataforma en beta abierta sin acuerdo de nivel de servicio documentado, lo que reduce su utilidad como referencia; el paso 5 retuvo a Azion y Bunny. \\ \hline
    Azure Front Door (Rules Engine / Edge Actions) & \parencite{microsoft2026frontdooredgeactions} & 2026-09-20 & Descartado (I2) & Rules Engine define reglas como condiciones de coincidencia más acciones predefinidas (encabezados, redirecciones, reescritura, caché); no ejecuta código del usuario. Edge Actions sí ejecuta código, pero está en vista previa y no es el propósito principal del producto. \\ \hline
\end{longtable}
}

\subsection*{D.5 Código abierto}

Ya nombra la ficha: Knative, OpenFaaS, KEDA, Fermyon Spin / SpinKube, wasmCloud, Wasmtime. Cada dato de precio se etiquetó como precio de lista, solo por cotización o no publicado; cada límite como publicado o no publicado; la URL y la fecha de consulta de cada fuente están en la entrada bibliográfica citada. En esta lista, \emph{vigente} (I1) equivale a proyecto mantenido con documentación oficial, y el precio es no aplicable por tratarse de software autoalojado.

{\scriptsize
\begin{longtable}{|p{2.5cm}|p{2.4cm}|p{1.4cm}|p{2.3cm}|p{5.2cm}|}
    \hline
    \textbf{Candidato} & \textbf{Fuente oficial} & \textbf{Fecha} & \textbf{Resultado (criterio)} & \textbf{Motivo / qué aporta} \\
    \hline
    \endhead
    Apache OpenWhisk & \parencite{openwhisk2026product,openwhisk2026limits} & 2026-09-20 & Descartado (I3) & Cumple I1 e I2 (proyecto Apache mantenido; valores por defecto publicados: tiempo 60.000~ms, memoria 256~MB, código 48~MB). Plataforma de funciones sobre Kubernetes del mismo tipo que Knative y OpenFaaS, ya en la ficha; no aporta ninguna de las seis dimensiones. \\ \hline
    Fission & \parencite{fission2026executors,fission2026releases} & 2026-09-20 & Entra (I1, I2, I3) & I3-2: el ejecutor se elige por función; poolmgr mantiene por defecto 3 pods genéricos precalentados y libera los especializados por tiempo de inactividad, mientras newdeploy y container escalan a cero con \texttt{minScale: 0}. Proyecto mantenido (versión estable v1.27.0, junio de 2026). Sin precio: autoalojado. \\ \hline
    Nuclio & \parencite{nuclio2026architecture,nuclio2026triggers,nuclio2026kafka,nuclio2026releases} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-2: consume flujos directamente, una partición de Kafka atendida por una sola réplica y un componente propio que reparte particiones). Con tres candidatos vivos de contraste distinto, el paso 5 retuvo a Fission (escala a cero, 2.2) y Fn Project (portabilidad, 2.6). \\ \hline
    Fn Project & \parencite{fnproject2026product,oracle2026fnoverview} & 2026-09-20 & Entra (I1, I2, I3) & I3-6: motor de funciones de código abierto sobre el que la documentación oficial de Oracle declara construido OCI Functions, lo que permite comparar el mismo motor autoalojado y gestionado (par con D.2, declarado en 3.1). Proyecto mantenido. Sin precio: autoalojado. \\ \hline
    WasmEdge & \parencite{wasmedge2026site,cncf2026wasmedge} & 2026-09-20 & Descartado (I3) & Cumple I1 e I2 (runtime WebAssembly, proyecto sandbox del CNCF). La ficha ya incluye Wasmtime, otro runtime WebAssembly; ser un segundo runtime no es ninguna de las seis dimensiones. \\ \hline
    Direktiv & \parencite{direktiv2026docs,direktiv2026github,direktiv2026landscape} & 2026-09-20 & Descartado (I2) & Motor de flujos dirigidos por eventos sobre Kubernetes y Knative, definidos en YAML. Su propósito principal es la orquestación de flujos, no una plataforma, runtime o autoescalador de cargas serverless; no cumple I2. \\ \hline
    Kubeless & \parencite{vmware2026kubeless} & 2026-09-20 & Descartado (X2) & Repositorio archivado por VMware; el README declara que ya no se mantiene. Archivado en GitHub el 15-12-2021 según el aviso del repositorio. \\ \hline
    riff & \parencite{projectriff2021final} & 2026-09-20 & Descartado (X2) & Proyecto declarado terminado el 13 de marzo de 2021. \\ \hline
    IronFunctions & \parencite{oracle2019fnproject} & 2026-09-20 & Descartado (X1) & Bifurcado en Fn Project por el mismo equipo al incorporarse a Oracle; Fn es su continuación y se evalúa en su lugar. \\ \hline
\end{longtable}
}

\subsection*{D.6 Datos sin servidor}

Ya nombra la ficha: Aurora Serverless, DynamoDB bajo demanda, Neon, Cloudflare D1. Cada dato de precio se etiquetó como precio de lista, solo por cotización o no publicado; cada límite como publicado o no publicado; la URL y la fecha de consulta de cada fuente están en la entrada bibliográfica citada.

{\scriptsize
\begin{longtable}{|p{2.5cm}|p{2.4cm}|p{1.4cm}|p{2.3cm}|p{5.2cm}|}
    \hline
    \textbf{Candidato} & \textbf{Fuente oficial} & \textbf{Fecha} & \textbf{Resultado (criterio)} & \textbf{Motivo / qué aporta} \\
    \hline
    \endhead
    PlanetScale & \parencite{planetscale2026pricing,planetscale2026hobbydeprecate} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-1: cobro por clúster mensual tras la deprecación del plan Hobby en 2024, frente al cobro por capacidad o solicitud de la ficha). Motor MySQL/Vitess gestionado, que solapa el eje SQL cubierto por Aurora y Neon; el paso 5 retuvo a Turso y Upstash. \\ \hline
    Turso (libSQL) & \parencite{turso2026pricing,turso2026usage} & 2026-09-20 & Entra (I1, I2, I3) & I3-1: cobro por filas leídas y escritas más almacenamiento (plan gratuito con 500~millones de filas leídas y 10~millones escritas al mes). I3-6: libSQL, bifurcación de SQLite con licencia MIT, puede ejecutarse fuera de la nube del proveedor. Motor SQLite distinto de los relacionales de la ficha. \\ \hline
    Upstash Redis & \parencite{upstash2026redisbilling,upstash2026redispricing} & 2026-09-20 & Entra (I1, I2, I3) & I3-1: cobro por comando (USD~0,20 por 100.000), con almacenamiento y ancho de banda aparte y máximo de 10.000 comandos por segundo; en bases globales cada escritura replicada cuenta como comando por región. Motor Redis, ausente de la ficha. \\ \hline
    CockroachDB Cloud (Basic) & \parencite{cockroach2026basicproduct,cockroach2026pricing} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-1: cobro por unidades de solicitud, USD~0,20 por millón, más almacenamiento; el plan Basic sucede al antiguo Serverless). SQL distribuido gestionado, que solapa el eje cubierto por Aurora; el paso 5 retuvo a Turso y Upstash. \\ \hline
    Firestore & \parencite{firestore2026pricing,firestore2026quotas,firestore2026enterprisepricing} & 2026-09-20 & Descartado (desempate, paso 5) & Cumple I1--I3 (I3-1: cobro por lecturas, escrituras y borrados por cada 100.000 operaciones). Base documental gestionada de Google, del mismo tipo que DynamoDB en la ficha; el paso 5 retuvo a Turso y Upstash. \\ \hline
    MongoDB Atlas Serverless & \parencite{mongodb2026flexmigration} & 2026-09-20 & Descartado (X2) & Sin creación de instancias desde febrero de 2025; fin de soporte el 22 de enero de 2026; instancias migradas a Free, Flex o Dedicated. \\ \hline
\end{longtable}
}

\subsection*{D.7 Declaración sobre listas sin alternativas adicionales}

Tras aplicar los criterios de la Tabla~\ref{tab:criterios-busqueda}, las cinco listas alcanzaron al menos dos alternativas que cumplen I1, I2 e I3 (tablas D.2 a D.6), por lo que no corresponde declarar ninguna lista sin alternativas. Las diez alternativas que entran se recogen en la Tabla~\ref{tab:alternativas-adicionales} de la sección~\ref{sec:alternativas}.

\clearpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% >>> DOCUMENTO ADJUNTO: CUESTIONARIO DE 30 PREGUNTAS
%%                              Responsable: todos (3-4 c/u); Daniel arma el
%%                              índice temático y ajusta la proporción.
%% Punto 3 de las Indicaciones:
%%   - 30 preguntas; formatos mezclados: selección múltiple, V/F, completar,
%%     respuesta corta;
%%   - cada una con respuesta correcta y breve justificación;
%%   - teoría y práctica; comprensión, aplicación y análisis crítico;
%%   - dificultad: 12 básicas, 12 intermedias, 6 avanzadas (40/40/20);
%%   - índice temático por sección del informe.
%% Redacción 100 % humana (punto 6.1). Numeración Q1, Q2, ... (la letra B
%% ya la usa el Anexo B).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\anexo{Q}{Documento adjunto: Cuestionario de evaluación de conocimientos}

\subsection*{Índice temático}

% Distribución PROPUESTA (borrador IA, formato): reparte 12/12/6 = 30 de
% modo que cada sección tenga al menos dos preguntas y las avanzadas caigan
% en las secciones con cifras propias o razonamiento (2.1–2.3, 3, 4.2, 4.3).
% Ajustar cuando existan las preguntas; los rangos se recalculan a mano.

{\scriptsize
\begin{longtable}{|p{5.6cm}|p{1.5cm}|p{1.5cm}|p{1.5cm}|p{1.5cm}|p{1.5cm}|}
    \hline
    \textbf{Sección del informe} & \textbf{Básicas} & \textbf{Interm.} & \textbf{Avanz.} & \textbf{Total} & \textbf{Rango} \\
    \hline
    \endhead
    1. Marco teórico y conceptual & 2 & 1 & 0 & 3 & 1--3 \\ \hline
    2.1 Modelos de ejecución y aislamiento & 1 & 1 & 1 & 3 & 4--6 \\ \hline
    2.2 Arranque en frío & 1 & 1 & 1 & 3 & 7--9 \\ \hline
    2.3 Facturación por consumo y punto de equilibrio & 1 & 1 & 1 & 3 & 10--12 \\ \hline
    2.4 Diseño orientado a eventos & 1 & 1 & 0 & 2 & 13--14 \\ \hline
    2.5 WebAssembly y WASI & 1 & 1 & 0 & 2 & 15--16 \\ \hline
    2.6 Límites técnicos y dependencia del proveedor & 1 & 1 & 0 & 2 & 17--18 \\ \hline
    3. Análisis comparativo (3.1 y 3.2) & 1 & 1 & 1 & 3 & 19--21 \\ \hline
    4.1 Prueba de concepto & 1 & 1 & 0 & 2 & 22--23 \\ \hline
    4.2 Modelo de costos & 1 & 1 & 1 & 3 & 24--26 \\ \hline
    4.3 Caso TERABYTE & 0 & 1 & 1 & 2 & 27--28 \\ \hline
    5. Tendencias y riesgos & 1 & 1 & 0 & 2 & 29--30 \\ \hline
    \textbf{Total} & \textbf{12} & \textbf{12} & \textbf{6} & \textbf{30} & 1--30 \\ \hline
\end{longtable}
}

\subsection*{Preguntas}

%% Formato sugerido para cada pregunta (no hay macro en la clase):
%%
%% \textbf{P1 --- Básica --- Selección múltiple --- Sección 2.1.}
%% Enunciado. \\
%% a) ... \quad b) ... \quad c) ... \quad d) ... \\
%% \textit{Respuesta:} b. \textit{Justificación:} una o dos frases.
%%
%% Agrupar por sección de origen, en el mismo orden del índice temático.

% TODO (todos)

\clearpage

\end{document}
