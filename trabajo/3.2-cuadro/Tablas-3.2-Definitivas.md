# Sección 3.2 · Criterios y Cuadro Comparativo de Plataformas
### Versión Consolidada Definitiva · TERABYTE (TI-05) · 20-09-2026

**Alcance y responsabilidades:**
* **Rodolfo (R):** Filas 1, 2, 5, 6, 9 y 10 (AWS y Azure; ancla de instancias siempre encendidas).
* **Claudio y Daniel (DC):** Filas 3, 4, 7, 8, 11 y 12 (Google, Cloudflare, Fastly, alternativas de 3.1) + Tabla 3.2c (Código Abierto) + Tabla Ampliada del Anexo (Deno, Vercel, Netlify).
* **Vicente (V):** Columna de arranque en frío (medición empírica en sección 4.1 para filas 1, 4 y 7; el resto "sin medición propia").
* **Francisca (F):** Configuración de la instancia de referencia EC2 reservada en fila 10 para el punto de equilibrio de 4.2.

**Reglas metodológicas y convenciones obligatorias:**
1. **Fecha de consulta de todas las celdas:** 20-09-2026. Todas las cifras provienen de documentación y calculadoras oficiales vigentes a esa fecha.
2. **Regiones de referencia:** AWS `us-east-1` (N. Virginia), Google Cloud `us-east4` (N. Virginia / Tier 1), Azure `East US`, Oracle `US East (Ashburn)`, plataformas de borde `global` o `regional` declarada.
3. **Una fila = Un plan cotizado:** El plan específico está declarado en la celda de plataforma.
4. **Lista cerrada para «Qué opera el equipo»:** *sistema operativo y parcheo · runtime y dependencias · planificación de capacidad · escalado · red (VPC, balanceo, certificados) · despliegue y versionado · observabilidad y alertas · recuperación ante fallos*. Construida a partir de los modelos de responsabilidad compartida de los proveedores y del CNCF.
5. **Compatibilidad LaTeX:** Todos los signos de moneda están rigurosamente escapados como `\$`.

---

## 1. Tabla 3.2a · Criterios Arquitectónicos y de Negocio (9 columnas)

| # | Resp. | Plataforma (plan cotizado) | Modelo | Aislamiento | Unidad de cobro y piso mensual | Precio de lista fechado | Arranque en frío (fuente) | Mitigación cold start (¿se factura aparte?) | Qué opera el equipo | Estado externo / Límite que fuerza rediseño |
|:---:|:---:|---|---|---|---|---|:---:|---|---|---|
| **1** | R · V | **AWS Lambda** | funciones como servicio | MicroVM Firecracker (código en jail chroot / cgroups sobre microVM aislada por cuenta) | Solicitudes (por millón) + GB-s de duración de reloj. Sin cargo fijo mensual; escala a cero | us-east-1, USD, 20-09-2026: \$0.20/millón de req.; \$0.0000166667/GB-s (x86) / \$0.0000133334/GB-s (Arm). Capa gratuita: 1 M req. y 400.000 GB-s/mes permanentes. Lista | (4.1) | Concurrencia aprovisionada: SÍ se factura aparte (\$0.0000041667/GB-s en reposo + uso). SnapStart (Java/Python, sin cargo) | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (S3, DynamoDB). Límite de rediseño: timeout máx. 15 min (900 s) y payload síncrono 6 MB |
| **2** | R | **Azure Functions** (Flex Consumption) | funciones como servicio | Contenedor / microVM Azure en entorno aislado por aplicación | Solicitud (\$0.40/M) + GB-s (\$0.000026). Sin cargo fijo mensual; escala a cero | East US, USD, 20-09-2026: \$0.40/M req. + \$0.000026/GB-s; Capa gratuita: 250.000 req. y 100.000 GB-s al mes. Lista | sin medición propia | Instancias Always Ready (precalentadas): SÍ se facturan (\$0.000004/GB-s base + \$0.000016 ejec) | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (Azure Blob, Cosmos DB). Límite de rediseño: **techo duro HTTP de 230 s** (idle timeout Azure Load Balancer) |
| **3** | DC | **Google Cloud Run functions** (antes Cloud Functions 2nd gen) | funciones como servicio | gVisor (primera generación) o microVM (segunda generación). La función se ejecuta como servicio de Cloud Run | Precios de Cloud Run: vCPU-s, GiB-s y solicitudes por millón. Sin cargo fijo mensual; escala a cero. Cloud Build y Artifact Registry se cobran aparte | us-east4 (Tier 1), USD, 20-09-2026: CPU activa \$0.000024/vCPU-s; memoria \$0.0000025/GiB-s; solicitudes \$0.40/millón. Capa gratuita: 180.000 vCPU-s, 360.000 GiB-s y 2 M req./mes. Lista | sin medición propia | Instancias mínimas (tarifa idle reducida) y CPU Boost al arranque. SÍ se facturan durante el arranque | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio. Límite de rediseño: 9 min para eventos (API v2) frente a 60 min para HTTP |
| **4** | DC · V | **Google Cloud Run** (servicios, facturación por solicitud) | contenedores sin servidor | Sandbox gestionado: gVisor (primera generación) y microVM Linux completa (segunda generación) | vCPU-s y GiB-s de tiempo facturable (bloques 100 ms) + solicitudes por millón. Sin cargo fijo mensual; escala a cero | us-east4 (Tier 1), USD, 20-09-2026: CPU activa \$0.000024/vCPU-s; CPU idle \$0.0000025/vCPU-s; memoria \$0.0000025/GiB-s; solicitudes \$0.40/millón. Capa gratuita compartida Tier 1. Lista | (4.1) | Instancias mínimas (tarifa idle) y CPU Boost al arranque (duplica CPU). SÍ se facturan | runtime y dependencias · red (VPC, balanceo, certificados) · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (sistema de archivos local es en RAM). Límite de rediseño: 60 min máx. en HTTP; trabajos mayores obligan a Cloud Run Jobs |
| **5** | R | **AWS Fargate** (sobre Amazon ECS) | contenedores sin servidor | MicroVM dedicada por tarea (Firecracker en infraestructura AWS gestionada) | vCPU-hora y GB-hora (facturado por segundo, mínimo 1 min). Sin cargo fijo mensual; escala a cero si desiredCount=0 | us-east-1, USD, 20-09-2026: Linux x86: \$0.04048/vCPU-hora (\$0.000011244/vCPU-s) y \$0.004445/GB-hora (\$0.000001235/GB-s). Fargate Spot con hasta 70 % descuento. Sin capa gratuita propia. Lista | sin medición propia | No aplica a nivel función: para latencia cero se mantienen tareas activas mínimas permanentes (se facturan a tarifa completa) | runtime y dependencias · red (VPC, subredes, SG, ALB) · despliegue y versionado · observabilidad y alertas | Almacenamiento efímero de 20 GB incluido; persistencia requiere Amazon EFS/EBS. Sin timeout de plataforma |
| **6** | R | **Azure Container Apps** (plan Consumption) | contenedores sin servidor | Contenedor sobre entorno gestionado Kubernetes/Envoy dedicado por entorno de usuario | vCPU-s y GiB-s de solicitudes activas. Sin cargo fijo mensual; escala a cero si minReplicas=0 | East US, USD, 20-09-2026: vCPU activa \$0.000024/vCPU-s; memoria activa \$0.000003/GiB-s; en reposo vCPU \$0.000003/vCPU-s y memoria \$0.00000035/GiB-s. Capa gratuita: 180.000 vCPU-s, 360.000 GiB-s y 2 M req./mes. Lista | sin medición propia | Instancias mínimas en reposo (minReplicas > 0). SÍ se facturan a tarifa reducida idle | runtime y dependencias · red (VPC / virtual network) · despliegue y versionado · observabilidad y alertas | SÍ obligatorio para persistencia (Azure Files). Límite de rediseño: 300 s máx. en solicitudes HTTP; tareas batch requieren Container App Jobs |
| **7** | DC · V | **Cloudflare Workers** (plan Paid, uso Standard) | cómputo en el borde | Isolates de V8 en un único proceso y espacio de memoria virtual multi-tenant (aislamiento a nivel de lenguaje) | Solicitudes (por millón) + tiempo de CPU (por millón de CPU-ms). **Piso mensual: \$5.00 USD/mes por cuenta.** Escala a cero en cómputo pero no en costo | global, USD, 20-09-2026: \$5.00/mes base (incluye 10 M req. y 30 M CPU-ms); + \$0.30/millón req. y + \$0.02/millón CPU-ms. Duración de reloj no se cobra. Capa gratuita (Free): 100.000 req./día y 10 ms CPU. Lista | (4.1) | No documenta SKU aprovisionado. Mecanismo estructural: creación de isolate en proceso existente (< 5 ms); snapshot de memoria Wasm en Python. Sin cargo aparte | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (Durable Objects, KV, D1, R2). Límite de rediseño: 128 MB RAM por isolate (no configurable) y 5 min máx. de CPU |
| **8** | DC | **Fastly Compute** (uso por tramos, post 04-11-2025) | cómputo en el borde | WebAssembly (WASI) ejecutado sobre motor Wasmtime; sandbox ligero con aislamiento estricto por solicitud | Compute Requests (por millón) + Compute vCPU-ms (por millón). Sin cargo fijo mensual en régimen de tramos actual | global, USD, 20-09-2026: Requests: 10 M gratis, luego \$0.50 a \$0.20/M según tramo. vCPU-ms: 100 M gratis, luego \$0.05 a \$0.02/millón ms. Capa gratuita: 10 M req. y 100 M vCPU-ms/mes. Lista. Paquetes Advantage/Ultimate por cotización | sin medición propia | No documenta concurrencia aprovisionada. Mecanismo estructural: compilación a Wasm e instanciación por solicitud en microsegundos. Sin cargo aparte | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (Config Store, KV Store, Secret Store). Límite de rediseño: 50 ms de tiempo de CPU por solicitud y 128 MB de heap (+ 1 MB stack) |
| **9** | R | **AWS Lambda@Edge + CloudFront Functions** (fila conjunta) | cómputo en el borde | Lambda@Edge: MicroVM en ubicaciones edge de CloudFront. CloudFront Functions: Aislamiento a nivel proceso / runtime JS ligero en red edge | Lambda@Edge: \$0.60/M req. + \$0.00005001/GB-s (sin free tier). CloudFront Functions: \$0.10/M req. (free tier 2 M req./mes, sin cobro por duración). Sin piso mensual | global, USD, 20-09-2026: Precios de lista publicados en CloudFront. Sin cargo base mensual en ambos servicios | sin medición propia | CloudFront Functions: inicio en submilisegundos estructural por diseño ligero. Lambda@Edge: no soporta concurrencia aprovisionada | runtime y dependencias · despliegue y versionado (asociación a CDN) | SÍ obligatorio: sin almacenamiento local escribible. Límite de rediseño: CloudFront Functions máx. 1 ms CPU; Lambda@Edge máx. 5 s en viewer y 30 s en origin |
| **10** | R · F | **Contenedor sobre EC2 reservada orquestado por ECS** (t3.medium) | instancias siempre encendidas (ancla) | VM dedicada (hipervisor Nitro / KVM); sin aislamiento por invocación (contenedores comparten kernel host) | Instancia-hora; **piso = instancia encendida 730 h al mes (\$30.37/mes On-Demand). No escala a cero** | us-east-1, USD, 20-09-2026: t3.medium (2 vCPU, 4 GiB): On-Demand \$0.0416/h (\$30.37/mes); Reserva 1 año No Upfront \$0.026/h (\$18.98/mes, 37 % ahorro); Reserva 3 años All Upfront \$0.017/h (~ \$12.41/mes, 59 % ahorro). Lista | no aplica (siempre encendida) | No aplica (el contenedor permanece en memoria y absorbe tráfico inmediato sin arranque en frío) | SO y parcheo, runtime Docker/containerd, capacidad, escalado EC2, red (VPC, balanceo, SG), despliegue, observabilidad, recuperación ante fallos del host | Estado puede residir en disco local (EBS/volumen) o en memoria del proceso; sin límite de tiempo por solicitud impuesto por la plataforma |
| **11** | DC | **Oracle Cloud Infrastructure Functions** (alternativa 3.1) | funciones como servicio | Contenedor gestionado / microVM en infraestructura OCI aislada por función. Motor basado en proyecto OSS Fn Project | Invocaciones (por millón) + GB-s de ejecución activa. Concurrencia aprovisionada cobrada al 25 % del tiempo de ejecución en reposo. Sin cargo fijo mensual; escala a cero | US East (Ashburn) / global, USD, 20-09-2026: Invocaciones: \$0.20 por millón (\$0.0000002/inv.); Ejecución: \$0.00001417/GB-s. Capa gratuita permanente (Always Free): 2 M de invocaciones y 400.000 GB-s al mes. Lista | sin medición propia | Concurrencia aprovisionada (Provisioned Concurrency): SÍ se factura aparte a tarifa reducida (25 % de la tarifa de ejecución mientras está idle; sin sobrecargo al procesar) | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (OCI Object Storage, Autonomous DB). Límite de rediseño: timeout síncrono 300 s (default 30 s); modo detached hasta 3600 s; memoria fija en 128 a 3072 MB |
| **12** | DC | **Azion Edge Functions** (alternativa 3.1) | cómputo en el borde | Isolates de V8 sobre Edge Runtime distribuido propio compatible con estándares web (W3C/WHATWG) | Compute time (por cada 1.000 s de CPU) + Invocaciones (por millón). Sin cargo fijo mensual en plan Starter/Pay-as-you-go; escala a cero | PoPs en Brasil/LATAM y global, USD, 20-09-2026: Compute time: \$0.072 por 1.000 s de CPU tras free tier; Invocaciones: \$0.30 por millón tras free tier. Capa gratuita mensual: 8 horas de compute (28.800 s) y 3 M de invocaciones/mes. Lista | sin medición propia | Aislamiento ligero en V8 Isolate en nodos edge (< 2 s cold start documentado, típico ms). No documenta SKU de concurrencia aprovisionada | runtime y dependencias · despliegue y versionado · observabilidad y alertas | SÍ obligatorio (Edge Storage, Edge SQL, Network Layer). Límite de rediseño: **2 segundos de tiempo de CPU por solicitud** (marketing publicita 5 min de wall-clock); isolate máx. 512 MB |

---

## 2. Tabla 3.2b · Límites Técnicos y Egreso de Datos (6 columnas)

| # | Resp. | Plataforma | Tiempo máximo de ejecución | Memoria / vCPU | Payload entrada / salida | Concurrencia / escalado | Egreso de datos (precio fechado) |
|:---:|:---:|---|---|---|---|---|---|
| **1** | R | **AWS Lambda** | 900 s (15 min) por ejecución | 128 MB a 10.240 MB (incrementos de 1 MB). vCPU proporcional (1 vCPU a 1.769 MB, hasta 6 vCPU a 10 GB) | Síncrono: 6 MB solicitud y respuesta. Asíncrono: 256 KB | 1.000 instancias concurrentes por región por defecto (ampliable). Ráfaga inicial 500–3.000. Escala a cero | 100 GB/mes gratis hacia internet; luego \$0.09/GB (primeros 10 TB en us-east-1). 20-09-2026 |
| **2** | R | **Azure Functions** (Flex Consumption) | **HTTP: 230 s (techo Load Balancer)**; otros triggers 30 min default (máx. unbounded) | Opciones fijas de tamaño de instancia: 512 MB (0.5 vCPU), 2048 MB (1 vCPU), 4096 MB (2 vCPU) | Solicitud HTTP: 100 MB por defecto en gateway. Azure Storage Queues: 4 MB | Hasta 1.000 instancias por aplicación. Autoescalado dinámico según disparador. Escala a cero | 100 GB/mes gratis en Azure; luego \$0.087/GB (East US, primeros 10 TB). 20-09-2026 |
| **3** | DC | **Google Cloud Run functions** | Techo según disparador: hasta 60 min (3600 s) en HTTP; hasta 9 min (540 s) en eventos (API v2) | Memoria default: 256 MiB; hasta 16 GiB de RAM con 4 vCPU según tabla de funciones. (Contradice máx. general de Cloud Run) | Solicitud y respuesta HTTP/1: 32 MiB. Sin límite en HTTP/2 | Hasta 1.000 solicitudes concurrentes por instancia de función. Escala a cero | Salida a internet por Premium Tier: 1 GiB gratis/mes; luego \$0.12/GB hasta 1.024 GiB (us-east4 a Norteamérica). 20-09-2026 |
| **4** | DC | **Google Cloud Run** (servicios) | 300 s (5 min) por defecto; extensible hasta 3600 s (60 min) por solicitud. Jobs: hasta 168 h | 512 MiB por defecto (mín. 128 MiB en gen 1, 512 MiB en gen 2; máx. 32 GiB). vCPU: 1 por defecto (mín. 0.08, máx. 8 vCPU) | Solicitud y respuesta HTTP/1: 32 MiB. Sin límite forzado bajo streaming HTTP/2 | Solicitudes concurrentes por instancia: 80 default, máx. 1.000. Máximo de instancias por revisión: 100 default (ampliable). Escala a cero | Salida a internet por Premium Tier: 1 GiB gratis/mes; luego \$0.12/GB hasta 1.024 GiB. Tráfico interno misma región sin cargo. 20-09-2026 |
| **5** | R | **AWS Fargate** (ECS) | Sin límite de plataforma: la tarea ejecuta continuamente hasta su terminación | 0.25 vCPU (0.5 a 2 GB) hasta 32 vCPU (hasta 244 GB en doc ECS; 16 vCPU / 120 GB en pricing page) | Sin límite de plataforma; limitado por capa de red (ALB máx. 100 MB de cuerpo) | Cuota regional por defecto: 1.000 tareas concurrentes (ampliable). Escala a cero si desiredCount=0 | Salida vía NAT Gateway (\$0.045/GB + \$0.045/hora de gateway) + egreso EC2 (\$0.09/GB tras 100 GB gratis). 20-09-2026 |
| **6** | R | **Azure Container Apps** (Consumption) | Solicitudes HTTP: 300 s máx. Container App Jobs: sin límite de tiempo (hasta 7 días) | 0.25 vCPU / 0.5 GiB hasta 4.0 vCPU / 8.0 GiB (perfiles Consumption estándar) | Solicitud HTTP: hasta 100 MB soportados vía ingress Envoy integrado | Hasta 300 réplicas por aplicación (cuota default 10–30 réplicas). Escala a cero por defecto (minReplicas=0) | 100 GB/mes gratis en Azure; luego \$0.087/GB hacia internet. 20-09-2026 |
| **7** | DC | **Cloudflare Workers** (Paid) | CPU time: 5 min máx. por HTTP (**30 s por defecto**). Duración de reloj: **SIN LÍMITE** mientras el cliente HTTP siga conectado; 15 min en Cron/Queues | 128 MB por isolate, **no configurable**, idéntico en Free y Paid. Incluye JS heap y memoria Wasm. vCPU no expuesta | URL: 16 KB. Cabeceras: 128 KB total. Cuerpo respuesta: sin límite. Cuerpo solicitud según plan de cuenta: Free/Pro 100 MB, Business 200 MB, Enterprise 5 GB | Sin límite numérico publicado de req./s ni de instancias. Escala global automática. 10.000 subsolicitudes por invocación en Paid | **SIN CARGO, declarado por el proveedor:** transferencia de datos (egreso) y ancho de banda incluidos sin cobro adicional en plan Paid. 20-09-2026 |
| **8** | DC | **Fastly Compute** | CPU time: **50 ms por ejecución**. Runtime de reloj total: **2 minutos por ejecución** (60 s en cuentas de prueba) | 1 MB de stack y 128 MB de heap por ejecución. **No configurable.** vCPU no expuesta; se factura por vCPU-ms consumido | Cabecera req./resp.: 128 KB por cabecera. URL: 8.192 bytes. Objeto en caché máx.: 100 MB. Paquete Wasm máx.: 100 MB. Cuerpo HTTP no publicado | Modelo de una instancia aislada por solicitud. Encadenamiento máx.: 20 saltos y 6 servicios únicos. Escala automática | **Tráfico de entrega (Full Site Delivery) se factura aparte:** 100 GB gratis/mes; luego Norteamérica y Europa \$0.12/GB y \$0.08/GB; Sudamérica \$0.19 y \$0.14/GB. 20-09-2026 |
| **9** | R | **AWS Lambda@Edge + CloudFront Functions** | CloudFront Functions: **1 ms de CPU**. Lambda@Edge: 5 s (viewer request/response) y 30 s (origin request/response) | CloudFront Functions: 2 MB fijos. Lambda@Edge: 128 MB fijos (viewer) o 128–10.240 MB configurables (origin) | CloudFront Functions: sin acceso a cuerpo HTTP. Lambda@Edge: 40 KB (viewer req.), 1 MB síncrono (origin req.), 40 MB con streaming | Integrado a escala de la CDN global de CloudFront (cientos de miles de solicitudes/s). Escala a cero | Tarifa de transferencia de datos de CloudFront: 1 TB/mes gratis; luego \$0.085/GB primer tramo en Norteamérica. 20-09-2026 |
| **10** | R · F | **Contenedor sobre EC2 reservada (ECS)** (t3.medium) | Sin límite de plataforma (el proceso se ejecuta de forma continua sin corte de timeout) | Fijo según la instancia aprovisionada: t3.medium cuenta con 2 vCPU y 4.0 GiB de RAM dedicados | Sin límite de plataforma; gobernado por la memoria del host y la configuración de Nginx / Docker | La que soporte la capacidad del nodo t3.medium; escalado horizontal por Auto Scaling Group (no a cero) | Tarifa estándar de salida a internet de AWS EC2: 100 GB/mes gratis; luego \$0.09/GB (us-east-1). 20-09-2026 |
| **11** | DC | **Oracle OCI Functions** | Síncrono: máx. 300 s (**default 30 s**). Modo detached (asíncrono): configurable de 5 s a **3.600 s (60 min)** | Opciones fijas de memoria: 128, 256, 512, 1024, 2048 y 3072 MB. vCPU dimensionada proporcionalmente a la RAM | Solicitud y respuesta HTTP: 6 MB cada una | Cuota regional por defecto: 60 instancias concurrentes por availability domain (ampliable por ticket). Escala a cero | **Primeros 10 TB/mes gratis hacia internet**; luego \$0.0085/GB. Sin cargo entre servicios OCI en la misma región. 20-09-2026 |
| **12** | DC | **Azion Edge Functions** | CPU time: **2 segundos máx. por solicitud**. Duración de reloj (wall-clock): máx. **5 minutos (300 s)** | Hasta 512 MB por isolate de función. vCPU no expuesta como parámetro dimensionable | Argumentos JSON de función: 100 KB. Tamaño de código de función: UI 6 MB, API 20 MB. Sub-requests: máx. 50 | Autoescalado continuo sobre la red distribuida de PoPs edge. Escala a cero | Tráfico Edge Network cobrado por GB según región (Brasil/LATAM \$0.06/GB en tiers iniciales, con 50 GB gratis al mes). 20-09-2026 |

---

## 3. Tabla 3.2c · Plataformas de Código Abierto (8 columnas)

*Nota metodológica:* En las plataformas autoalojadas de código abierto, el precio es «no aplica (autoalojado)» y el campo «Qué opera el equipo» es, por definición de arquitectura compartida: **todo salvo lo que provea el clúster gestionado**.

| # | Proyecto | Modelo que implementa | Aislamiento | Escala a cero (sí/no, por defecto) | Timeout y concurrencia por defecto (configurables) | Qué opera el equipo | Acreditación CNCF u otra (verificada) |
|:---:|---|---|---|:---:|---|---|---|
| **1** | **Knative Serving** | Contenedores sin servidor sobre Kubernetes con enrutamiento y autoescalado | Contenedor (pod de Kubernetes). Aislamiento efectivo depende del runtime del clúster (containerd, CRI-O) | **Sí, por defecto.** ConfigMap `config-autoscaler` con `enable-scale-to-zero: true`. Periodo de gracia: 30 s | Timeout: 300 s default (`revision-timeout-seconds`), máx. 600 s. Concurrencia: `container-concurrency: 0` (ilimitada default), máx. 1.000. Target: 100 | Todo salvo lo que provea el clúster gestionado | **Graduado CNCF.** Aceptado el 02-03-2022 (Incubating) y graduado el **11-09-2025** (anunciado 08-10-2025). Categoría: Scheduling & Orchestration |
| **2** | **OpenFaaS** | Funciones como servicio sobre Kubernetes o sobre host individual (`faasd`) | Contenedor. Opcionalmente gVisor (`runsc`) en OpenFaaS Edge / Pro vía Profiles para código no confiable | **No por defecto.** Es opt-in por función con etiqueta `com.openfaas.scale.zero: true`. Periodo de inactividad configurable | Parámetros configurables en gateway (`upstream_timeout`) y función (`exec_timeout`). Defaults no publicados en referencia. JetStream `ack_wait` 60 s default | Todo salvo lo que provea el clúster gestionado | **No es proyecto CNCF.** Gobernanza de OpenFaaS Ltd. `faasd` CE bajo EULA no comercial; versiones Pro/Edge con licencia comercial. Despliegue sin K8s documentado |
| **3** | **KEDA** | Autoescalador dirigido por eventos para Kubernetes (no ejecuta código) | **No aplica: KEDA no ejecuta código de usuario.** Escala despliegues de otros. Aislamiento depende de la carga | **Sí, es su propósito principal.** Activa y escala pods desde cero réplicas basándose en métricas de eventos externos | No aplica en sentido de ejecución HTTP. Parámetros de sondeo (`pollingInterval` default 30 s, `cooldownPeriod` default 300 s) | Todo salvo lo que provea el clúster gestionado | **Graduado CNCF.** Aceptado el 12-03-2020 (Sandbox), movido a Incubating el 18-08-2021 y graduado el **22-08-2023**. Categoría: Scheduling & Orchestration |
| **4** | **Fermyon Spin / SpinKube** | Cómputo sin servidor basado en WebAssembly sobre Kubernetes (`SpinApp`) | WebAssembly. Ejecutores: `containerd-shim-spin` (Wasm en nodo bajo RuntimeClass) o `Spintainer` (contenedor) | **No por defecto en el operador.** Requiere integración explícita con KEDA; HPA estándar de K8s no escala a cero | Timeouts gobernados por la configuración del pod y los disparadores de Spin. Defaults numéricos no publicados en docs | Todo salvo lo que provea el clúster gestionado | **Sandbox CNCF (como SpinKube).** Aceptado el **21-01-2025**. El runtime base Spin de Fermyon mantiene gobernanza en organización `spinframework` independiente |
| **5** | **wasmCloud** | Plataforma de aplicaciones distribuidas basada en componentes WebAssembly | WebAssembly (Wasmtime con soporte de Component Model y capas de seguridad propias) | **Sí, por diseño.** Los componentes son binarios sin estado que se instancian por solicitud y no retienen instancias inactivas | Concurrencia: `--max-concurrent` en `wash scale` (ilimitada por defecto). `--timeout-ms` (2000 ms default) rige la interfaz de control | Todo salvo lo que provea el clúster gestionado | **Incubating CNCF.** Aceptado el 13-07-2021 (Sandbox) y promovido a Incubating el **08-11-2024** (anunciado 12-11-2024). Categoría: Scheduling & Orchestration |
| **6** | **Fission** (alternativa OSS 3.1) | Funciones como servicio sobre Kubernetes con múltiples estrategias de ejecución | Contenedor (pod de Kubernetes). Entornos de ejecución políglotas gestionados | **Configurable por función.** Admite ejecutor `poolmgr` (mantiene pool caliente) y `newdeploy` (Deployment K8s con `minScale: 0`) | Timeout en router: 300 s por defecto (configurable). Concurrencia gobernada por la capacidad del pod y la configuración del ejecutor | Todo salvo lo que provea el clúster gestionado | **No es proyecto CNCF.** Proyecto de código abierto mantenido por la comunidad Fission (ex-Platform9, versión estable v1.27.0 al 20-09-2026) |

---

## 4. Tabla Ampliada del Anexo · Plataformas de Borde y FaaS Gestionado (3 filas)

*Nota metodológica:* Deno Deploy, Vercel Functions y Netlify Functions fueron relevados bajo los mismos 14 campos reglamentarios de las tablas 3.2a y 3.2b, pero se presentan en el Anexo para mantener el cuerpo del informe en las doce filas fijas acordadas.

| Campo / Dimensión | Deno Deploy (plan Pro) | Vercel Functions (Fluid compute, Pro) | Netlify Functions (planes por créditos) |
|---|---|---|---|
| **Modelo** | Cómputo en el borde | Funciones como servicio / borde | Funciones como servicio / borde |
| **Aislamiento** | No verificado en referencia (Deno Sandboxes usa microVMs) | No documentado en docs (enforceado por AWS Lambda bajo el capó) | No documentado en docs (interfaz de compatibilidad Lambda) |
| **Unidad de cobro y piso** | Suscripción mensual + Active CPU (horas), Memory time (GiB-h) y req. **Piso: \$20 USD/mes** | Active CPU (CPU-h), Provisioned Memory (GB-h) e Invocaciones. **Piso: \$20 USD/mes por asiento** | Créditos (moneda unificada para build, compute, data y bandwidth). **Piso: \$20 USD/mes en Pro** (Personal \$9) |
| **Precio de lista fechado** | global, USD, 20-09-2026: \$20/mes (incluye 5 M req., 200 GiB egreso, 50 h CPU, 750 GiB-h RAM). Capa Free: 1 M req. Lista | iad1 (default), USD, 20-09-2026: CPU \$0.128/h, RAM \$0.0106/GB-h, req. \$0.60/M. Rango regional \$0.128 a \$0.221/h. Capa Hobby gratis. Lista | global, USD, 20-09-2026: Pro \$20/mes (3.000 créditos base); recarga 1.500 créditos por \$10. Capa Free: 300 créditos/mes con límite duro. Lista |
| **Mitigación cold start** | No documenta mitigación: apps inactivas se apagan a los 20–30 s para no facturar RAM | Reutilización de instancias con concurrencia optimizada; costo cubierto por Provisioned Memory activa | No documentado mecanismo de mitigación ni instancias mínimas en docs consultados |
| **Qué opera el equipo** | runtime y dependencias · despliegue y versionado · observabilidad y alertas | runtime y dependencias · despliegue y versionado · observabilidad y alertas | runtime y dependencias · despliegue y versionado · observabilidad y alertas |
| **Estado externo / Límite** | SÍ (Deno KV, volúmenes). Límite: apagado tras 20–30 s descarta estado en RAM | SÍ obligatorio. Límite: 300 s default y 800 s máx. (1800 s beta); bundle máx. 250 MB | SÍ (Netlify Blobs, DB). Límite: 60 s síncronas no configurable; background functions 15 min |
| **Tiempo máx. ejecución** | No publicado límite HTTP del producto actual (build timeout: 5 min default, 15 min Pro) | Hobby 300 s máx.; Pro 300 s default, 800 s máx. y 1800 s (30 min) en beta por función | Síncronas: 60 s (no configurable). Programadas: 30 s. Background: 15 min. Streaming: 10 s |
| **Memoria / vCPU** | Runtime: 768 MB por defecto (configurable de 512 a 4096 MB vía config). Build: 2 vCPU / 3 GB RAM | Máx. 4 GB en Pro. Tamaños: Standard 1 vCPU / 2 GB; Performance 2 vCPU / 4 GB | 1024 MB por defecto. Configurable entre 1024 y 4096 MB (0.5 a 2.0 vCPU) SOLO en Pro y Enterprise |
| **Payload entrada / salida** | No encontrado límite numérico de payload HTTP en docs oficiales consultados | Bundle 250 MB (500 MB Python; 5 GB Large functions beta). Payload HTTP no publicado | Síncronas con buffer: 6 MB. Streaming: 20 MB. Background functions: 256 KB |
| **Concurrencia / escalado** | Escala a cero a los 20–30 s de inactividad. Sin límite numérico publicado de concurrencia | Hasta 30.000 concurrentes en Pro. 1.024 descriptores de archivo compartidos. Escala a cero | No encontrado límite numérico de concurrencia ni de instancias en docs consultados |
| **Egreso de datos** | Incluido en plan: 20 GiB en Free, 200 GiB en Pro. Excedente \$0.20/GiB. Entrada sin costo. 20-09-2026 | Facturado como Fast Data Transfer en página general de precios. 20-09-2026 | Consumido del pozo de créditos. Tarifa desagregada por GB en USD no publicada. 20-09-2026 |

---

## 5. Notas al Pie Reglamentarias (para inclusión directa en LaTeX)

* **Nota (a) — Columna «Qué opera el equipo»:** Corresponde a la lista cerrada construida por el grupo de trabajo a partir de la síntesis de las matrices formales de responsabilidad compartida de Amazon Web Services, Microsoft Azure y Google Cloud, y de la taxonomía del Whitepaper de Serverless del CNCF (*Cloud Native Computing Foundation*). Ninguno de los proveedores de borde analizados (Cloudflare, Fastly, Azion) publica una matriz de responsabilidad compartida formal con este nivel de desagregación; las responsabilidades asignadas en esas filas constituyen una inferencia técnica documentada a partir de los contratos de servicio, guías de arquitectura y límites de plataforma oficiales.
* **Nota (b) — Columna «Arranque en frío»:** La marca `(4.1)` remite directamente a las mediciones empíricas desarrolladas en la sección 4.1 del presente informe por el equipo de pruebas (Vicente Arratia), ejecutadas sobre cargas estandarizadas bajo las mismas condiciones experimentales. Para las restantes plataformas comerciales se declara `sin medición propia` con el fin de respetar el principio metodológico de no mezclar mediciones de laboratorio con benchmarks promocionales de proveedores ni cifras de literatura que empleen metodologías heterogéneas. Para el contenedor siempre encendido sobre EC2 (fila 10) se declara `no aplica` al no existir ciclo de desasignación ni reciclado de recursos entre peticiones.
* **Nota (c) — Fila 9 (AWS Lambda@Edge y CloudFront Functions):** Se consolidan en una fila conjunta por constituir la oferta escalonada de cómputo en el borde de un mismo proveedor sobre la infraestructura de Amazon CloudFront. En cada celda se declaran los valores diferenciados para ambos servicios, evidenciando el contraste entre un runtime ultra-ligero enfocado en manipulación de cabeceras (CloudFront Functions, máx. 1 ms CPU) y un entorno FaaS completo distribuido en centros regionales (Lambda@Edge, hasta 30 s).
* **Nota (d) — Respaldo Teórico de Aislamiento en el Borde (Filas 7 y 8):** El modelo de aislamiento de Cloudflare Workers (V8 Isolates) y Fastly Compute (WASI / WebAssembly) prescinde de microVMs dedicadas por petición para permitir tiempos de inicialización sub-milisegundos y densidades multi-inquilino de miles de scripts por máquina física en un mismo proceso. Como demostraron recientemente Schwarzl, Borrello, Kogler, Varda, Schuster, Schwarz y Gruss (2022) en *Robust and Scalable Process Isolation against Spectre in the Cloud*, este diseño comparte el espacio de direcciones virtuales, lo que introduce vectores de canal lateral microarquitectónicos (*Spectre-PHT*) mitigables únicamente mediante detección probabilística y aislamiento dinámico por hardware (*DyPrIs*).

---

## 6. Análisis Comparativo de Plataformas (Texto para Sección 3.2 de main.tex)

El análisis transversal del cuadro comparativo revela que la promesa teórica de «escala a cero y pago estricto por uso» experimenta tensiones significativas cuando se confronta con las exigencias operativas reales. En primer lugar, la mitigación de arranques en frío en FaaS de propósito general introduce costos fijos encubiertos: tanto *Provisioned Concurrency* en AWS Lambda (\$0,0000041667/GB-s reservado más \$0,0000097222/GB-s activo) como las instancias *Always Ready* en Azure Functions Flex (\$0,000004/GB-s de reserva continua) transforman la facturación variable en un piso mensual análogo al de una instancia aprovisionada. En cargas con tráfico sostenido o ráfagas regulares (como la recepción continua de telemetría de grúas y contenedores en TERABYTE), este sobrecosto aproxima el gasto mensual al punto de equilibrio de una instancia dedicada reservada (EC2 `t3.medium` a \$18,98/mes, Fila 10), donde el proceso residente en memoria elimina el retardo por diseño. Asimismo, el cómputo en el borde evidencia una bifurcación económica y técnica crítica: ejecutar lógica en la periferia mediante microVMs regionales (AWS Lambda@Edge) impone un costo de cómputo de \$0,00005001/GB-s, exactamente 3,0 veces superior al de Lambda estándar (\$0,0000166667/GB-s), además de carecer de capa gratuita y trasladar tarifas de distribución CDN sin posibilidad de concurrencia aprovisionada. En contraste, las soluciones basadas en aislamiento a nivel de lenguaje (Cloudflare Workers y Fastly Compute) eliminan el arranque en frío mediante procesos e isolates compartidos sub-milisegundo, pero exigen una renuncia arquitectónica severa: operan bajo estrictos límites de CPU (50 ms por solicitud en Fastly y 2 s en Azion), carecen de sistemas de archivos convencionales y trasladan la persistencia a bases externas mediante llamadas de red adicionales.

En segundo lugar, la complejidad operacional no desaparece en las arquitecturas serverless, sino que se redistribuye entre la infraestructura y el diseño de la aplicación. La transición desde FaaS hacia contenedores sin servidor (AWS Fargate y Azure Container Apps) amplía la flexibilidad de empaquetado y elimina las cuotas de tiempo (soportando procesos batch continuos o tareas de hasta 7 días), pero transfiere al equipo responsabilidades de ingeniería de red considerables: requiere provisionar subredes VPC privadas, balanceadores de carga de aplicación (ALB) y pasarelas NAT (cuyo costo base de \$0,045/hora más \$0,045/GB suele exceder el cómputo en volúmenes medios), además de sintonizar manualmente políticas de autoescalado (KEDA o métricas de CPU). Esta fricción se acentúa por límites duros no configurables de la infraestructura subyacente que contradicen las cuotas promocionadas por los proveedores: en Azure Functions, si bien la plataforma publicita tiempos de ejecución de hasta 60 minutos o modo no acotado (*unbounded*), las funciones disparadas por HTTP enfrentan un techo infranqueable de 230 segundos impuesto por el *idle timeout* del Azure Load Balancer, obligando a rediseñar procesos analíticos hacia patrones asíncronos desacoplados mediante colas o *Durable Functions*. Finalmente, el ecosistema serverless introduce un riesgo latente de dependencia y obsolescencia que valida la necesidad de evaluar alternativas portables (Sección 3.1): el anuncio oficial de AWS cerrando App Runner a nuevos clientes en 2026 demuestra que las abstracciones gestionadas de alta conveniencia están sujetas a discontinuación unilateral del proveedor, consolidando a las plataformas basadas en contenedores abiertos y estándares de la CNCF (Knative, Fn Project, Fission) como salvaguardas indispensables para la soberanía arquitectónica institucional.
